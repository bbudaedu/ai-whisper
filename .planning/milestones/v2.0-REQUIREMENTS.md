# Requirements Archive: v2.0 (2026-03-29)

**全系統自動化 E2E 測試與穩定性增強**

## 🧩 Requirements Status

| ID | Requirement | Status | Outcome | Evidence |
|----|-------------|--------|---------|----------|
| **TEST-01** | API 自動化測試 (Backend) | ✅ | Validated | `tests/v2/test_auth.py`, `test_tasks_crud.py`, `test_download.py` |
| **TEST-02** | Pipeline 整合測試 (Processing) | ✅ | Validated | `tests/v2/test_pipeline.py`, `test_gpu_concurrency.py` |
| **TEST-03** | Web UI E2E 測試 (Frontend) | ✅ | Validated | `web-ui-external/e2e/login.spec.ts`, `task_flow.spec.ts` |
| **TEST-04** | 環境與基礎設施 | ✅ | Validated | `tests/v2/conftest.py`, `test_config.v2.json`, `playwright.config.ts` |
| **DB-01** | 說話者名稱資料庫欄位 | ✅ | Validated | `pipeline/queue/models.py` schema 更新，支援索引。 |
| **API-01** | PATCH 編輯端點 | ✅ | Validated | `PATCH /api/tasks/{task_id}` 具備 RBAC 保護。 |
| **UI-01** | Web UI 行內編輯 | ✅ | Validated | `TaskTracker.tsx` 支援 inline 編輯與即時儲存。 |
| **LLM-01** | Prompt 講者資訊注入 | ✅ | Validated | `auto_proofread.py` 成功接收並注入講者資訊。 |

## 🎯 Original Goals Result
建立一套涵蓋 legacy 功能與 v1.0 新功能的自動化測試框架，確保全系統（API, Pipeline, Web UI）在持續疊代中的穩定性。
**結果**: 100% 達成。現在具備雙層自動化測試網，為後續 v3.0 的開發提供了穩定性保障。

## 🔧 Technical Adjustments
- **Mocking**: 決定在 UI E2E 中使用 Playwright `route` Mocking 而非連結真實後端，以確保前端測試速度與穩定性。
- **GPU Lock**: 驗證了 `flock` 跨進程互斥行為，修正了原本對同進程二次鎖定的斷言假設。
- **Dependencies**: 新增了 `python-multipart`, `email-validator`, `argon2-cffi`, `itsdangerous` 等測試依賴套件。

---
*Archived from REQUIREMENTS.md on 2026-03-29*
