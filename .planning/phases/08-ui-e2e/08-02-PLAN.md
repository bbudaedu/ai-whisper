---
phase: 08-ui-e2e
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified: [web-ui-external/src/pages/SubmitTask.tsx, web-ui-external/src/pages/TaskTracker.tsx, web-ui-external/e2e/task_flow.spec.ts]
autonomous: true
requirements: [TEST-03]

must_haves:
  truths:
    - "User can submit a YouTube task via UI and see success message"
    - "Task appears in Task Tracker with correct initial status"
    - "UI correctly reflects status changes and provides download links when done"
    - "Responsive design verified (Mobile Nav works as expected)"
  artifacts:
    - path: "web-ui-external/e2e/task_flow.spec.ts"
      provides: "E2E tests for Task submission, tracking, and responsive layout"
  key_links:
    - from: "task_flow.spec.ts"
      to: "SubmitTask.tsx"
      via: "data-testid selectors for form submission"
    - from: "task_flow.spec.ts"
      to: "TaskTracker.tsx"
      via: "data-testid selectors for status verification"
---

<objective>
實作任務流與響應式佈局的 E2E 測試。

Purpose: 驗證核心業務流程（提交 -> 追蹤 -> 下載）在不同裝置上的正確性。
Output: 覆蓋完整任務生命週期的 E2E 腳本。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-ui-e2e/08-01-SUMMARY.md
@web-ui-external/src/pages/SubmitTask.tsx
@web-ui-external/src/pages/TaskTracker.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 為任務提交與追蹤頁面添加 data-testid</name>
  <files>web-ui-external/src/pages/SubmitTask.tsx, web-ui-external/src/pages/TaskTracker.tsx</files>
  <action>
    手動添加以下 `data-testid`：
    - `SubmitTask.tsx`: `input-youtube-url`, `btn-submit-task`, `success-alert`, `error-alert`, `mode-youtube`, `mode-upload`。
    - `TaskTracker.tsx`: `task-list-table`, `task-item-{id}`, `task-status-{id}`, `btn-refresh`, `btn-download-all`。
  </action>
  <verify>
    <automated>grep -r "data-testid" web-ui-external/src/pages/SubmitTask.tsx web-ui-external/src/pages/TaskTracker.tsx</automated>
  </verify>
  <done>關鍵 UI 元素具備 data-testid。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 實作任務流 E2E 測試</name>
  <files>web-ui-external/e2e/task_flow.spec.ts</files>
  <behavior>
    - 測試 1 (Submit): 提交一個有效的 YouTube URL，驗證出現成功訊息。
    - 測試 2 (Track): 前往「任務追蹤」頁面，驗證新提交的任務出現在列表中。
    - 測試 3 (Status): 模擬任務完成 (可透過 API 手動更新資料庫狀態或等待 mock 處理)，驗證 UI 狀態更新為「已完成」並顯示下載連結。
    - 測試 4 (Responsive): 在 Mobile Viewport 下，驗證底部導覽列 (Bottom Nav) 可見且能正常切換頁面。
  </behavior>
  <action>
    實作 `task_flow.spec.ts`。使用 `storageState: '.auth/user.json'` 以跳過登入步驟。
    針對行動版測試，使用 Playwright 的 `Mobile Safari` 專案配置。
  </action>
  <verify>
    <automated>cd web-ui-external && npx playwright test e2e/task_flow.spec.ts</automated>
  </verify>
  <done>任務流測試通過，且已覆蓋桌面與行動版佈局驗證。</done>
</task>

</tasks>

<verification>
1. 執行 `cd web-ui-external && npm run test:e2e`。
2. 確認所有測試 (包含跨瀏覽器) 皆通過。
</verification>

<success_criteria>
- 完整任務流程（提交、追蹤、狀態變化）自動化驗證完成。
- 響應式佈局在 E2E 中得到基礎覆蓋。
- 測試執行穩定，無 Flaky 現象。
</success_criteria>

<output>
After completion, create `.planning/phases/08-ui-e2e/08-02-SUMMARY.md`
</output>
