---
phase: 08-ui-e2e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [web-ui-external/playwright.config.ts, web-ui-external/src/pages/Login.tsx, web-ui-external/src/components/Navigation.tsx, web-ui-external/e2e/auth.setup.ts, web-ui-external/e2e/login.spec.ts]
autonomous: true
requirements: [TEST-03, TEST-04]

must_haves:
  truths:
    - "Playwright is installed and configured to run against Vite dev server"
    - "User can login successfully and session is stored in .auth/user.json"
    - "Unauthenticated users are redirected to /login when accessing protected routes"
  artifacts:
    - path: "web-ui-external/playwright.config.ts"
      provides: "E2E test configuration"
    - path: "web-ui-external/e2e/auth.setup.ts"
      provides: "Global authentication setup for session reuse"
    - path: "web-ui-external/e2e/login.spec.ts"
      provides: "Login and Auth protection tests"
  key_links:
    - from: "playwright.config.ts"
      to: "npm run dev"
      via: "webServer command"
    - from: "login.spec.ts"
      to: "Login.tsx"
      via: "data-testid selectors"
---

<objective>
建立 Playwright 測試基礎設施並實作登入與身份驗證保護的 E2E 測試。

Purpose: 確保 UI 的入口安全與 Session 管理機制穩定。
Output: 運作正常的 Playwright 配置、具備測試標籤的登入元件、以及自動化登入測試腳本。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-ui-e2e/08-CONTEXT.md
@.planning/phases/08-ui-e2e/08-RESEARCH.md
@web-ui-external/package.json
@web-ui-external/src/pages/Login.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 安裝與配置 Playwright</name>
  <files>web-ui-external/playwright.config.ts, web-ui-external/package.json</files>
  <action>
    在 `web-ui-external` 目錄安裝 `@playwright/test`。
    建立 `playwright.config.ts`，配置如下：
    - `webServer`: 執行 `npm run dev`，等待 `http://localhost:5173`。
    - `baseURL`: `http://localhost:5173`。
    - `projects`:
      - 'setup': 匹配 `auth.setup.ts`。
      - 'chromium': 使用 `Desktop Chrome`，依賴 'setup'。
      - 'Mobile Safari': 使用 `iPhone 12`，依賴 'setup'。
    - `storageState`: 指向 `.auth/user.json`。
    並在 `package.json` 加入 `"test:e2e": "playwright test"`。
  </action>
  <verify>
    <automated>cd web-ui-external && npx playwright --version</automated>
  </verify>
  <done>Playwright 環境建立完成，config 檔案存在且包含 webServer 配置。</done>
</task>

<task type="auto">
  <name>Task 2: 為登入與導覽元件添加 data-testid</name>
  <files>web-ui-external/src/pages/Login.tsx, web-ui-external/src/components/Navigation.tsx</files>
  <action>
    依據 D-30，手動添加以下 `data-testid`：
    - `Login.tsx`: `login-email`, `login-password`, `login-submit`, `google-signin-button` (id 欄位已存在，確認其可被 locator 找到)。
    - `Navigation.tsx`: `nav-logout`, 以及各導覽連結 (如 `nav-submit`, `nav-history`)。
  </action>
  <verify>
    <automated>grep -r "data-testid" web-ui-external/src/pages/Login.tsx web-ui-external/src/components/Navigation.tsx</automated>
  </verify>
  <done>關鍵 UI 元素具備 data-testid，方便 Playwright 定位。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 實作 Auth Setup 與 Login 測試</name>
  <files>web-ui-external/e2e/auth.setup.ts, web-ui-external/e2e/login.spec.ts</files>
  <behavior>
    - auth.setup.ts: 執行登入流程，將 storageState 存入 .auth/user.json。
    - login.spec.ts:
      - 測試 1: 未登入狀態訪問 `/` 應重導向至 `/login`。
      - 測試 2: 輸入無效憑證應顯示錯誤訊息。
      - 測試 3: 登入成功後應跳轉至首頁，且導覽列顯示使用者 Email。
  </behavior>
  <action>
    實作 E2E 腳本。注意測試前需確保後端 API Server 已啟動 (建議使用 Phase 07 建立的測試環境)。
    登入測試時使用測試專用的 `test@example.com` / `password123` (需先確保資料庫有此資料)。
  </action>
  <verify>
    <automated>cd web-ui-external && npx playwright test e2e/login.spec.ts</automated>
  </verify>
  <done>Auth 測試全數通過，Session 成功持久化。</done>
</task>

</tasks>

<verification>
1. 執行 `cd web-ui-external && npm run test:e2e -- --project=chromium`
2. 確認所有 Auth 測試通過。
3. 手動檢查 `.auth/user.json` 是否產生 (且已被 .gitignore 忽略)。
</verification>

<success_criteria>
- Playwright 成功驅動 Vite dev server。
- 登入流程自動化完成且穩定。
- 跨瀏覽器 (Desktop/Mobile) 的 Auth 邏輯通過驗證。
</success_criteria>

<output>
After completion, create `.planning/phases/08-ui-e2e/08-01-SUMMARY.md`
</output>
