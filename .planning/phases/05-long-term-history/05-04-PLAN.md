---
phase: 05
plan: 04
type: execute
wave: 2
depends_on: [03]
files_modified: [api/routers/tasks.py, api/schemas.py]
autonomous: true
requirements: [STOR-01, STOR-02]
must_haves:
  truths:
    - "任務狀態 API 回傳包含事件歷程與產出檔案列表"
    - "歷史列表 API 支援基礎的分頁與過濾"
  artifacts:
    - path: "api/schemas.py"
      provides: "包含事件與產出檔案的 Pydantic 模型"
  key_links:
    - from: "api/routers/tasks.py"
      to: "pipeline/queue/repository.py"
      via: "get_events, get_artifacts"
---

<objective>
擴充任務 API 以提供完整的歷史資訊。讓任務詳情 API 回傳事件歷程（Timeline）與產出檔案（Artifacts），並優化歷史列表 API。

Purpose: 滿足 D-06 與 D-07 的需求，為前端提供充足的歷史查詢資料。
Output: 擴充後的 Pydantic 模式、整合事件與產出的 API 端點。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-long-term-history/05-CONTEXT.md
@api/routers/tasks.py
@api/schemas.py
@pipeline/queue/repository.py
@pipeline/queue/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 擴充 API Schema 定義</name>
  <read_first>
    <file>api/schemas.py</file>
    <file>pipeline/queue/models.py</file>
  </read_first>
  <files>api/schemas.py</files>
  <action>
    1. 在 api/schemas.py 中新增 TaskEventSchema 與 TaskArtifactSchema。
       - TaskEventSchema: id, event_type, metadata, created_at。
       - TaskArtifactSchema: id, format, path, created_at。
    2. 更新 TaskStatusResponse，新增選填欄位：
       - events: list[TaskEventSchema] = []
       - artifacts: list[TaskArtifactSchema] = []
  </action>
  <verify>
    <automated>grep -q "class TaskEventSchema" api/schemas.py && grep -q "artifacts:" api/schemas.py</automated>
  </verify>
  <acceptance_criteria>
    - TaskStatusResponse 包含 events 與 artifacts 列表。
  </acceptance_criteria>
  <done>API Schema 擴充完成</done>
</task>

<task type="auto">
  <name>Task 2: 整合事件與產出至任務詳情 API</name>
  <read_first>
    <file>api/routers/tasks.py</file>
    <file>pipeline/queue/repository.py</file>
  </read_first>
  <files>api/routers/tasks.py</files>
  <action>
    修改 api/routers/tasks.py 中的 get_task_status 端點：
    1. 使用 TaskRepository 取得指定任務的 events 與 artifacts。
    2. 將取得的資料填充至 TaskStatusResponse 回傳。
    3. 確保歷史列表 API (get_task_history) 維持輕量，僅回傳基本狀態。
  </action>
  <verify>
    <automated>grep -A 10 "get_task_status" api/routers/tasks.py | grep "get_events"</automated>
  </verify>
  <acceptance_criteria>
    - get_task_status API 回傳的 JSON 包含事件歷程與檔案列表。
  </acceptance_criteria>
  <done>任務詳情 API 資料整合完成</done>
</task>

</tasks>

<verification>
啟動 API Server 並使用 curl 或瀏覽器存取 /api/tasks/{task_id}，驗證回傳結果包含 artifacts 與 events。
</verification>

<success_criteria>
1. 任務詳情 API 能正確顯示該任務的所有處理節點（事件）。
2. 任務詳情 API 能列出該任務產出的所有檔案格式與路徑。
</success_criteria>

<output>
After completion, create `.planning/phases/05-long-term-history/05-04-SUMMARY.md`
</output>
