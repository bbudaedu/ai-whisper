---
phase: 05
plan: 05
type: execute
wave: 3
depends_on: [04]
files_modified: [web-ui-external/src/pages/TaskHistory.tsx, web-ui-external/src/App.tsx, web-ui-external/src/components/Navigation.tsx, web-ui-external/src/pages/Dashboard.tsx]
autonomous: true
requirements: [STOR-01, STOR-02]
must_haves:
  truths:
    - "使用者可在 Web UI 看到歷史任務列表"
    - "使用者可透過 UI 下載歷史任務的產出檔案"
  artifacts:
    - path: "web-ui-external/src/pages/TaskHistory.tsx"
      provides: "歷史任務查詢介面"
  key_links:
    - from: "web-ui-external/src/pages/TaskHistory.tsx"
      to: "/api/tasks/history"
      via: "fetch/axios GET"
---

<objective>
建立外部 Web UI 的歷史查詢與下載介面。整合後端已具備的 /api/tasks/history 端點，讓使用者能回溯與下載過去完成的任務。

Purpose: 提供視覺化的歷史紀錄管理，落實「隨時下載先前成果」的需求。
Output: TaskHistory 頁面組件、更新後的導覽選單與路由配置。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-long-term-history/05-CONTEXT.md
@web-ui-external/src/pages/TaskTracker.tsx
@web-ui-external/src/components/Navigation.tsx
@api/routers/tasks.py
@api/routers/download.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 實作 TaskHistory 頁面</name>
  <read_first>
    <file>web-ui-external/src/pages/TaskTracker.tsx</file>
    <file>web-ui-external/src/pages/Dashboard.tsx</file>
  </read_first>
  <files>web-ui-external/src/pages/TaskHistory.tsx</files>
  <action>
    1. 建立 web-ui-external/src/pages/TaskHistory.tsx，參考 TaskTracker.tsx 的表格結構。
    2. 使用 axios 或 fetch 呼叫 /api/tasks/history。
    3. 實作任務列表，顯示：標題、建立時間、狀態、操作（下載）。
    4. 實作展開詳情：顯示來源、錯誤訊息（若有）、下載按鈕（指向 /api/tasks/{task_id}/download）。
    5. 確保支援深色/淺色模式，並具備 mobile-first 響應式佈局。
  </action>
  <verify>
    <automated>grep -q "TaskHistory" web-ui-external/src/pages/TaskHistory.tsx && grep -q "/api/tasks/history" web-ui-external/src/pages/TaskHistory.tsx</automated>
  </verify>
  <acceptance_criteria>
    - TaskHistory.tsx 組件已建立。
    - 指向正確的 API 端點 (/api/tasks/history)。
    - 包含下載連結與狀態標籤。
  </acceptance_criteria>
  <done>歷史任務頁面開發完成</done>
</task>

<task type="auto">
  <name>Task 2: 配置導覽與路由</name>
  <read_first>
    <file>web-ui-external/src/App.tsx</file>
    <file>web-ui-external/src/components/Navigation.tsx</file>
  </read_first>
  <files>web-ui-external/src/App.tsx, web-ui-external/src/components/Navigation.tsx, web-ui-external/src/pages/Dashboard.tsx</files>
  <action>
    1. 在 web-ui-external/src/App.tsx 中註冊路由 /history 指向 TaskHistory 組件。
    2. 在 web-ui-external/src/components/Navigation.tsx 中新增「歷史記錄」連結（使用 Lucide 圖示，如 History 或 Library）。
    3. 在 web-ui-external/src/pages/Dashboard.tsx 中新增指向 /history 的快捷按鈕。
  </action>
  <verify>
    <automated>grep -q "history" web-ui-external/src/App.tsx && grep -q "歷史記錄" web-ui-external/src/components/Navigation.tsx</automated>
  </verify>
  <acceptance_criteria>
    - App.tsx 包含歷史路由。
    - 導覽列包含歷史選單。
    - Dashboard 有通往歷史的入口。
  </acceptance_criteria>
  <done>導覽與路由連結配置完成</done>
</task>

</tasks>

<verification>
手動檢查 Web UI 是否出現「歷史記錄」選單，且點擊後能載入資料並顯示下載按鈕。
</verification>

<success_criteria>
1. 使用者可透過選單進入歷史任務列表。
2. 歷史列表能正確顯示已完成、處理中與失敗的歷史任務。
3. 對於 DONE 狀態的歷史任務，可下載產出壓縮檔。
</success_criteria>

<output>
After completion, create `.planning/phases/05-long-term-history/05-05-SUMMARY.md`
</output>
