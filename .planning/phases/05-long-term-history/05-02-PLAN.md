---
phase: 05-long-term-history
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - api/routers/tasks.py
  - pipeline/notebooklm_tasks.py
autonomous: true
requirements:
  - STOR-01
  - STOR-02
user_setup: []

must_haves:
  truths:
    - "Task lifecycle changes are automatically logged in SQLite"
    - "Artifact outputs are registered upon creation"
  artifacts:
    - path: "api/routers/tasks.py"
      provides: "Event registration hook during API request"
    - path: "pipeline/notebooklm_tasks.py"
      provides: "Artifact registration hook during stage completion"
  key_links:
    - from: "pipeline/notebooklm_tasks.py"
      to: "database/persistence.py"
      via: "register_artifact function call"

---

<objective>
將持久化邏輯整合進既有的任務處理流程中，確保任務建立、狀態更新與成果產出時同步寫入資料庫。

Purpose: 實作 Event-Driven Persistence，確保任務生命週期完全被記錄 (滿足 D-07, D-12)。
Output: 更新任務處理 API 與 Pipeline 模組，加入資料庫寫入掛鉤。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/05-long-term-history/05-CONTEXT.md
@.planning/phases/05-long-term-history/05-RESEARCH.md
@.planning/phases/05-long-term-history/05-01-PLAN.md
@api/routers/tasks.py
@pipeline/notebooklm_tasks.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate Task Lifecycle Logging</name>
  <files>api/routers/tasks.py</files>
  <read_first>
    - api/routers/tasks.py
    - database/persistence.py
  </read_first>
  <action>
    Modify `api/routers/tasks.py` tasks creation and update endpoints:
    - Add `log_task_event(task_id, 'created', metadata)` when task is created.
    - Add `log_task_event(task_id, 'updated', metadata)` when status changes to 'pending', 'running', 'done', 'failed', 'cancelled'.
  </action>
  <verify>
    <automated>curl -X POST /api/tasks ... && sqlite3 database.db "SELECT * FROM task_events"</automated>
  </verify>
  <acceptance_criteria>
    - `task_events` table contains 'created' event after task submission.
  </acceptance_criteria>
  <done>API hooks integrated with persistence logging.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Artifact Registration</name>
  <files>pipeline/notebooklm_tasks.py</files>
  <read_first>
    - pipeline/notebooklm_tasks.py
    - database/persistence.py
  </read_first>
  <action>
    Modify `pipeline/notebooklm_tasks.py` stage completion logic:
    - Import `register_artifact` from `database/persistence.py`.
    - Call `register_artifact(task_id, format, path)` when a result file is generated (e.g., txt, srt, json).
  </action>
  <verify>
    <automated>pytest tests/test_notebooklm_e2e.py && sqlite3 database.db "SELECT * FROM task_artifacts"</automated>
  </verify>
  <acceptance_criteria>
    - `task_artifacts` table contains entries after pipeline tasks complete.
  </acceptance_criteria>
  <done>Pipeline hooks integrated with artifact registration.</done>
</task>

</tasks>

<verification>
透過執行端對端測試 `pytest tests/test_notebooklm_e2e.py` 並查詢資料庫，確保任務生命週期事件與產出檔案被正確記錄。
</verification>

<success_criteria>
現有任務系統能無縫寫入長期儲存庫，無需額外手動操作。
</success_criteria>
