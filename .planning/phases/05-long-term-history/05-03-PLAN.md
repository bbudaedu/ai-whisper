---
phase: 05
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [pipeline/queue/models.py, pipeline/queue/repository.py, database/persistence.py, scripts/migrate_to_unified_db.py]
autonomous: true
requirements: [STOR-01, STOR-02]
must_haves:
  truths:
    - "任務事件與產出檔案紀錄儲存在統一的資料庫（task_queue.db）中"
    - "既有 persistence.py 介面不再依賴獨立的 database.db"
  artifacts:
    - path: "pipeline/queue/models.py"
      provides: "TaskEvent 與 TaskArtifact SQLModel"
    - path: "scripts/migrate_to_unified_db.py"
      provides: "資料遷移工具"
  key_links:
    - from: "database/persistence.py"
      to: "pipeline/queue/database.py"
      via: "get_session"
---

<objective>
統一持久化資料層。將分散在 database.db (sqlite3) 與 task_queue.db (SQLModel) 的資料模型整合至 task_queue.db，並更新相關 Repository 與工具類。

Purpose: 消除資料碎片化，確保任務、事件與產出檔案具備完整的關聯性與查詢效能。
Output: 整合後的 SQLModel 模型、更新後的 TaskRepository、以及資料遷移腳本。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-long-term-history/05-CONTEXT.md
@.planning/phases/05-long-term-history/05-RESEARCH.md
@pipeline/queue/models.py
@pipeline/queue/repository.py
@database/persistence.py
@database/schema.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: 更新模型定義與 Repository</name>
  <read_first>
    <file>pipeline/queue/models.py</file>
    <file>pipeline/queue/repository.py</file>
    <file>database/schema.sql</file>
  </read_first>
  <files>pipeline/queue/models.py, pipeline/queue/repository.py</files>
  <action>
    1. 在 pipeline/queue/models.py 中新增 TaskEvent 與 TaskArtifact 類別（繼承 SQLModel, table=True）。
       - TaskEvent: id (int, pk), task_id (int, fk:tasks.id), event_type (str), metadata (str/JSON), created_at (datetime)。
       - TaskArtifact: id (int, pk), task_id (int, fk:tasks.id), format (str), path (str), created_at (datetime)。
    2. 擴充 Task 模型：
       - 新增 audio_profile: Optional[str] = Field(default=None)
       - 新增 source_metadata: Optional[str] = Field(default=None) # 儲存 JSON string
    3. 在 pipeline/queue/repository.py 的 TaskRepository 中新增方法：
       - add_event(task_id: int, event_type: str, metadata: dict = None)
       - add_artifact(task_id: int, format: str, path: str)
       - get_events(task_id: int) -> list[TaskEvent]
       - get_artifacts(task_id: int) -> list[TaskArtifact]
  </action>
  <verify>
    <automated>grep -q "class TaskEvent" pipeline/queue/models.py && grep -q "class TaskArtifact" pipeline/queue/models.py</automated>
  </verify>
  <acceptance_criteria>
    - pipeline/queue/models.py 包含 TaskEvent 與 TaskArtifact 定義。
    - TaskRepository 包含 add_event 與 add_artifact 方法。
  </acceptance_criteria>
  <done>資料模型與 Repository 方法擴充完成</done>
</task>

<task type="auto">
  <name>Task 2: 重構 persistence.py 橋接器</name>
  <read_first>
    <file>database/persistence.py</file>
    <file>pipeline/queue/database.py</file>
    <file>pipeline/queue/repository.py</file>
  </read_first>
  <files>database/persistence.py</files>
  <action>
    修改 database/persistence.py，使其不再直接使用 sqlite3 開啟 database.db，而是呼叫 TaskRepository：
    1. 移除 get_db_connection 與 init_db (不再需要獨立的 database.db)。
    2. 重構 log_task_event：使用 pipeline.queue.database.get_session() 與 TaskRepository(session).add_event()。
    3. 重構 register_artifact：使用 get_session() 與 TaskRepository(session).add_artifact()。
    確保參數相容性，使既有呼叫端（如 api/routers/tasks.py, pipeline/notebooklm_scheduler.py）無需修改。
  </action>
  <verify>
    <automated>grep -v "sqlite3.connect" database/persistence.py | grep "get_session"</automated>
  </verify>
  <acceptance_criteria>
    - database/persistence.py 不再包含 sqlite3 直接連接邏輯。
    - log_task_event 與 register_artifact 內部改用 SQLModel session。
  </acceptance_criteria>
  <done>持久化工具類重構完成，指向統一資料庫</done>
</task>

<task type="auto">
  <name>Task 3: 實作資料遷移與環境清理</name>
  <read_first>
    <file>database/persistence.py</file>
  </read_first>
  <files>scripts/migrate_to_unified_db.py</files>
  <action>
    1. 建立 scripts/migrate_to_unified_db.py，讀取舊的 database.db 中的 tasks, task_events, task_artifacts 資料。
    2. 將資料逐筆寫入新的 data/task_queue.db 中對應的表（注意 ID 匹配）。
    3. 執行後，重新命名或刪除舊的 database.db。
    4. 確保 api_server.py 啟動時會建立新的 SQLModel 表（create_db_and_tables 已存在於 lifespan）。
  </action>
  <verify>
    <automated>python3 scripts/migrate_to_unified_db.py --check</automated>
  </verify>
  <acceptance_criteria>
    - 遷移腳本可執行。
    - 舊資料成功整合至統一資料庫。
  </acceptance_criteria>
  <done>資料遷移完成，系統使用單一資料庫</done>
</task>

</tasks>

<verification>
執行 pytest tests/test_persistence.py 驗證重構後的 persistence 介面功能正常。
</verification>

<success_criteria>
1. 所有任務事件與產出檔案均儲存在單一的 data/task_queue.db 中。
2. 系統中不再產生新的 database.db 檔案。
3. 既有任務流程不受影響且能正常寫入事件與產出。
</success_criteria>

<output>
After completion, create `.planning/phases/05-long-term-history/05-03-SUMMARY.md`
</output>
