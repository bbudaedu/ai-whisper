---
phase: 05-long-term-history
plan: 03
type: execute
wave: 3
depends_on: [05-02]
files_modified:
  - api/routers/tasks.py
  - web-ui/src/components/TaskHistory.tsx
autonomous: false
requirements:
  - STOR-01
  - STOR-02
user_setup: []

must_haves:
  truths:
    - "API returns a list of history tasks"
    - "Web UI displays task history with status and download link"
  artifacts:
    - path: "api/routers/tasks.py"
      provides: "New GET /api/tasks/history endpoint"
    - path: "web-ui/src/components/TaskHistory.tsx"
      provides: "Frontend component to render history list"
  key_links:
    - from: "web-ui/src/components/TaskHistory.tsx"
      to: "/api/tasks/history"
      via: "API fetch"

---

<objective>
實作歷史任務查詢 API 與對應的 Web UI 歷史列表頁面。

Purpose: 滿足 D-09, D-10 需求，讓使用者可查詢與回溯歷史任務。
Output: 新增歷史查詢 API 端點與 Web UI 列表組件。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/05-long-term-history/05-01-PLAN.md
@.planning/phases/05-long-term-history/05-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement History API</name>
  <files>api/routers/tasks.py</files>
  <read_first>
    - api/routers/tasks.py
  </read_first>
  <action>
    Add `GET /api/tasks/history` to `api/routers/tasks.py`:
    - Queries `tasks` table for current user.
    - Implements basic pagination (page=1, size=20) and sorting (created_at DESC).
    - Returns JSON list of task objects including status, created_at, source_type.
  </action>
  <verify>
    <automated>curl -H "Authorization: Bearer <token>" /api/tasks/history</automated>
  </verify>
  <acceptance_criteria>
    - API returns 200 OK with task list for authenticated user.
  </acceptance_criteria>
  <done>History retrieval API implemented.</done>
</task>

<task type="checkpoint:human-verify">
  <what-built>歷史任務列表 UI</what-built>
  <how-to-verify>
    1. 登入 Web UI。
    2. 點擊「歷史記錄」選單。
    3. 確認清單正確顯示任務狀態、時間與下載入口。
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
透過 Web UI 與 API 交互驗證，確認歷史資料顯示正確且具備下載功能。
</verification>

<success_criteria>
使用者能從 Web UI 順利查詢個人歷史任務並下載先前成果。
</success_criteria>
