---
phase: 05-long-term-history
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/schema.sql
  - database/persistence.py
  - tests/test_persistence.py
autonomous: true
requirements:
  - STOR-01
  - STOR-02
user_setup: []

must_haves:
  truths:
    - "Task table can store all required fields"
    - "Task events and artifacts are linked to tasks"
    - "Database connection supports WAL mode"
  artifacts:
    - path: "database/schema.sql"
      provides: "New SQLite schema for tasks, events, and artifacts"
    - path: "database/persistence.py"
      provides: "Persistence utility functions"
  key_links:
    - from: "database/persistence.py"
      to: "database/schema.sql"
      via: "DDL execution"

---

<objective>
建立長期任務儲存的基礎架構與資料庫 Schema，包含任務、事件與成果的三大資料表設計。

Purpose: 確保所有任務及其生命週期資訊可被持久化儲存，滿足 STOR-01 與 STOR-02 需求。
Output: 包含 `tasks`, `task_events`, `task_artifacts` 的 SQLite Schema 及基礎 persistence 工具函數。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/05-long-term-history/05-CONTEXT.md
@.planning/phases/05-long-term-history/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define SQLite Schema</name>
  <files>database/schema.sql</files>
  <read_first>
    - database/schema.sql
  </read_first>
  <action>
    Create `database/schema.sql` with:
    - `tasks`: id (uuid), owner_id, source_type, audio_profile, status, created_at, finished_at, error_message, retry_count
    - `task_events`: id, task_id (FK), event_type, metadata (json), created_at
    - `task_artifacts`: id, task_id (FK), format, path, created_at
    Ensure indexes on task_id for events and artifacts.
  </action>
  <verify>
    <automated>sqlite3 database.db < database/schema.sql && sqlite3 database.db ".tables"</automated>
  </verify>
  <acceptance_criteria>
    - Tables `tasks`, `task_events`, `task_artifacts` must exist in database.
  </acceptance_criteria>
  <done>Schema file created and verified in DB.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Persistence Utilities</name>
  <files>database/persistence.py, tests/test_persistence.py</files>
  <read_first>
    - database/schema.sql
    - database/persistence.py
  </read_first>
  <behavior>
    - Initialize connection with WAL mode enabled.
    - Test task creation and event logging works.
  </behavior>
  <action>
    Create `database/persistence.py`:
    - `get_db_connection()`: Sets `PRAGMA journal_mode=WAL;`.
    - `log_task_event(task_id, event_type, metadata)`: Inserts into `task_events`.
    - `register_artifact(task_id, format, path)`: Inserts into `task_artifacts`.
  </action>
  <verify>
    <automated>pytest tests/test_persistence.py</automated>
  </verify>
  <acceptance_criteria>
    - `pytest` passes all tests.
    - Database is in WAL mode (verify via PRAGMA query).
  </acceptance_criteria>
  <done>Persistence utilities implemented and tested.</done>
</task>

</tasks>

<verification>
執行 `pytest tests/test_persistence.py` 確認所有資料庫操作與 WAL 設定生效。
</verification>

<success_criteria>
資料庫 Schema 完整且 persistence 工具函數能正確記錄任務與檔案連結。
</success_criteria>
