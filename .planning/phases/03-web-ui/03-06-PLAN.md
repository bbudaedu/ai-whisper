---
phase: 03-web-ui
plan: 06
type: execute
wave: 5
depends_on: ["03-05", "03-04"]
files_modified:
  - web-ui-external/src/pages/Playlists.tsx
  - web-ui-external/src/pages/Settings.tsx
  - web-ui-external/src/App.tsx
autonomous: true
requirements:
  - UI-01
  - UI-04
  - PROC-02
  - NOTF-01
must_haves:
  truths:
    - "Users can add and manage YouTube playlists for tracking"
    - "Users can configure email notification settings"
  artifacts:
    - path: "web-ui-external/src/pages/Playlists.tsx"
      provides: "Playlist management interface"
    - path: "web-ui-external/src/pages/Settings.tsx"
      provides: "Notification and user settings"
  key_links:
    - from: "web-ui-external/src/pages/Playlists.tsx"
      to: "Backend API"
      via: "Playlist endpoint"
      pattern: "api.post.*/api/playlists"
---

<objective>
Implement the Playlist tracking manager (D-13) and the Settings page for configuring Email notifications (D-08, NOTF-01).

Purpose: Enable automated batch processing through YouTube playlists and ensure users can receive email alerts upon completion.
Output: Playlist management view and user settings view.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-web-ui/03-CONTEXT.md
@.planning/phases/03-web-ui/03-RESEARCH.md
@.planning/phases/03-web-ui/03-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Playlist Manager</name>
  <files>
    - web-ui-external/src/pages/Playlists.tsx
    - web-ui-external/src/App.tsx
  </files>
  <read_first>
    - web-ui/src/components/PlaylistTaskManager.tsx
    - web-ui-external/src/api/client.ts
  </read_first>
  <action>
    1. Create `web-ui-external/src/pages/Playlists.tsx`.
    2. Build an input field to add new playlist URLs. Include an Add button. POST to `/api/playlists`.
    3. Fetch and display existing tracked playlists in a card/list format showing title, status, and latest episode count.
    4. Provide toggle switches to enable/disable tracking for each playlist (PUT to `/api/playlists/{id}/control`). Provide a delete button with a confirmation dialog.
    5. Wire `<Playlists />` into `App.tsx` for the "playlists" tab.
  </action>
  <acceptance_criteria>
    - `web-ui-external/src/pages/Playlists.tsx` contains logic to add, toggle, and delete playlists
    - UI includes confirmation dialog text "刪除播放清單：顯示確認對話框" or similar intent
  </acceptance_criteria>
  <verify>
    <automated>cd web-ui-external && npm run build</automated>
  </verify>
  <done>Users can manage YouTube playlist tracking from the external UI.</done>
</task>

<task type="auto">
  <name>Task 2: Build Settings Page</name>
  <files>
    - web-ui-external/src/pages/Settings.tsx
    - web-ui-external/src/App.tsx
  </files>
  <read_first>
    - web-ui-external/src/api/client.ts
  </read_first>
  <action>
    1. Create `web-ui-external/src/pages/Settings.tsx`.
    2. Build a form to manage Email notification preferences (D-08). Include an input for "Notification Email".
    3. Include a "Save" button that POSTs changes to the user preferences endpoint (or `/api/config` if mapping to global).
    4. Add a "Logout" button that clears the `auth_token` from localStorage and resets AuthContext state.
    5. Wire `<Settings />` into `App.tsx` for the "settings" tab.
  </action>
  <acceptance_criteria>
    - `web-ui-external/src/pages/Settings.tsx` contains an email input for notifications
    - Settings page includes a logout mechanism
  </acceptance_criteria>
  <verify>
    <automated>cd web-ui-external && npm run build</automated>
  </verify>
  <done>User preferences for notifications are configurable.</done>
</task>

</tasks>

<verification>
Verify forms exist for playlists and settings and that they submit to the configured API client.
</verification>

<success_criteria>
- Playlist URLs can be added and tracking toggled
- Email notifications can be configured
- User can successfully log out from the settings page
</success_criteria>

<output>
After completion, create `.planning/phases/03-web-ui/03-06-SUMMARY.md`
</output>
