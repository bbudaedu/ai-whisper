---
phase: 03-web-ui
plan: 05
type: execute
wave: 4
depends_on: ["03"]
files_modified:
  - web-ui-external/src/pages/TaskTracker.tsx
  - web-ui-external/src/App.tsx
  - web-ui-external/src/hooks/usePolling.ts
autonomous: true
requirements:
  - UI-01
  - UI-06
  - UI-07
  - NOTF-02
must_haves:
  truths:
    - "Users can see a list of their tasks and current status"
    - "Task status updates automatically via polling"
    - "Users can manually refresh the task list"
    - "Task rows can be expanded to show detailed processing steps"
    - "Completed tasks show a Zip download and individual format download links"
  artifacts:
    - path: "web-ui-external/src/pages/TaskTracker.tsx"
      provides: "Task list and status display"
    - path: "web-ui-external/src/hooks/usePolling.ts"
      provides: "Polling logic for real-time updates"
  key_links:
    - from: "web-ui-external/src/pages/TaskTracker.tsx"
      to: "Backend API"
      via: "Polling hook"
      pattern: "api.get.*/api/tasks"
---

<objective>
Implement the Task Tracking interface (D-14) with automatic polling (D-15) and download capabilities for completed tasks (D-16).

Purpose: Allow users to monitor the progress of their transcription jobs and download results.
Output: Real-time task list view and result download logic.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-web-ui/03-CONTEXT.md
@.planning/phases/03-web-ui/03-RESEARCH.md
@.planning/phases/03-web-ui/03-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Polling Hook</name>
  <files>
    - web-ui-external/src/hooks/usePolling.ts
  </files>
  <read_first>
    - web-ui-external/src/api/client.ts
  </read_first>
  <action>
    1. Create a custom hook `web-ui-external/src/hooks/usePolling.ts` using `useEffect` and `setInterval`.
    2. The hook should accept an API endpoint and a polling interval (default 10000ms per D-15), and return a function to trigger a manual refresh.
    3. Ensure `clearInterval` is called in the cleanup function to prevent memory leaks (Pitfall 1 from research).
    4. Manage loading, data, and error states.
  </action>
  <acceptance_criteria>
    - `web-ui-external/src/hooks/usePolling.ts` exports a custom hook
    - Hook contains `setInterval` and `clearInterval`
  </acceptance_criteria>
  <verify>
    <automated>cd web-ui-external && npm run build</automated>
  </verify>
  <done>Polling logic is encapsulated and safe from memory leaks.</done>
</task>

<task type="auto">
  <name>Task 2: Build Task Tracker View</name>
  <files>
    - web-ui-external/src/pages/TaskTracker.tsx
    - web-ui-external/src/App.tsx
  </files>
  <read_first>
    - web-ui/src/components/TaskTracker.tsx
    - web-ui-external/src/hooks/usePolling.ts
  </read_first>
  <action>
    1. Create `web-ui-external/src/pages/TaskTracker.tsx`.
    2. Use the `usePolling` hook to fetch tasks from `/api/tasks` (Phase 2 query API).
    3. Render a table/list view of tasks. For each task, display title, status badge (pending/running/done/failed), and submission time. Implement an expandable details view (D-14) that shows processing steps when a row is clicked.
    4. For tasks with status 'done', render a "下載全部(Zip)" button that triggers a download via the Phase 2 API (`/api/task/{id}/download`), AND display a list of individual formats with corresponding download links (UI-07, NOTF-02).
    5. Add a "手動更新" (Manual Refresh) button that triggers the manual refresh function from the usePolling hook (D-15).
    6. Wire `<TaskTracker />` into `App.tsx` for the `/track` route.
  </action>
  <acceptance_criteria>
    - `web-ui-external/src/pages/TaskTracker.tsx` maps over task data to render rows
    - Completed tasks show a download action
  </acceptance_criteria>
  <verify>
    <automated>cd web-ui-external && npm run build</automated>
  </verify>
  <done>Task list displays correctly and updates automatically.</done>
</task>

</tasks>

<verification>
Ensure the task list correctly handles empty states and gracefully recovers from polling errors.
</verification>

<success_criteria>
- Polling updates task state every 10 seconds
- Status badges are visible
- Downloads can be initiated for finished tasks
</success_criteria>

<output>
After completion, create `.planning/phases/03-web-ui/03-05-SUMMARY.md`
</output>
