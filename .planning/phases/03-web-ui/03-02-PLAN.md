---
phase: 03-web-ui
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - web-ui-external/src/auth/AuthContext.tsx
  - web-ui-external/src/pages/Login.tsx
  - web-ui-external/src/App.tsx
  - web-ui-external/src/components/ProtectedRoute.tsx
autonomous: true
requirements:
  - UI-02
must_haves:
  truths:
    - "Users can log in using Email/Password"
    - "Authentication state persists across page reloads"
    - "Unauthenticated users are redirected to the Login page"
  artifacts:
    - path: "web-ui-external/src/auth/AuthContext.tsx"
      provides: "Authentication state management"
    - path: "web-ui-external/src/pages/Login.tsx"
      provides: "Login UI"
    - path: "web-ui-external/src/components/ProtectedRoute.tsx"
      provides: "Route guard for authenticated areas"
  key_links:
    - from: "web-ui-external/src/pages/Login.tsx"
      to: "Backend Auth API"
      via: "API Client POST to /token"
      pattern: "api.post"
---

<objective>
Implement the authentication flow for the external Web UI, supporting Email/Password login (D-05) and persistent sessions (D-07). Note: Google OAuth integration is stubbed here for future implementation once backend endpoints are ready.

Purpose: Secure the external UI so only authorized users can submit tasks and view results.
Output: Auth context, login page, and protected routing logic.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-web-ui/03-CONTEXT.md
@.planning/phases/03-web-ui/03-RESEARCH.md
@.planning/phases/03-web-ui/03-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup AuthContext and ProtectedRoute</name>
  <files>
    - web-ui-external/src/auth/AuthContext.tsx
    - web-ui-external/src/components/ProtectedRoute.tsx
    - web-ui-external/src/App.tsx
  </files>
  <read_first>
    - web-ui-external/src/api/client.ts
    - web-ui-external/src/App.tsx
  </read_first>
  <action>
    1. Create `web-ui-external/src/auth/AuthContext.tsx` to provide `isAuthenticated`, `user`, `login`, and `logout` functions. It should read/write the token to `localStorage` (key: `auth_token`).
    2. Create `web-ui-external/src/components/ProtectedRoute.tsx` that checks `isAuthenticated` from AuthContext. If false, it renders a Login view; if true, it renders its `children`.
    3. Update `web-ui-external/src/App.tsx` to wrap the main application layout in `<AuthProvider>` and `<ProtectedRoute>`.
  </action>
  <acceptance_criteria>
    - `web-ui-external/src/auth/AuthContext.tsx` contains `createContext` and `localStorage.getItem('auth_token')`
    - `web-ui-external/src/components/ProtectedRoute.tsx` returns children when authenticated
  </acceptance_criteria>
  <verify>
    <automated>cd web-ui-external && npm run build</automated>
  </verify>
  <done>Authentication state is managed globally and protects the main app content.</done>
</task>

<task type="auto">
  <name>Task 2: Build Login UI</name>
  <files>
    - web-ui-external/src/pages/Login.tsx
    - web-ui-external/src/components/ProtectedRoute.tsx
  </files>
  <read_first>
    - web-ui-external/src/api/client.ts
    - web-ui-external/src/auth/AuthContext.tsx
  </read_first>
  <action>
    1. Create `web-ui-external/src/pages/Login.tsx` with a centered card layout containing an Email and Password form.
    2. Use Tailwind classes conforming to UI-SPEC (e.g., `bg-white dark:bg-[#161b22]`, `text-[#3B82F6]` for the primary CTA "登入").
    3. Handle form submission by calling the login function from AuthContext, which should issue a `POST` request to `/api/token` (Phase 2 Auth API). Include error handling (e.g., "發生錯誤，請稍後再試或重新整理頁面。").
    4. Provide a placeholder/stub button for Google OAuth login ("Google 登入") that logs a console message for now.
    5. Update `ProtectedRoute.tsx` to render the `<Login />` component when unauthenticated.
  </action>
  <acceptance_criteria>
    - `web-ui-external/src/pages/Login.tsx` contains a form with email and password inputs
    - `web-ui-external/src/pages/Login.tsx` calls the auth API on submit
  </acceptance_criteria>
  <verify>
    <automated>cd web-ui-external && npm run build</automated>
  </verify>
  <done>Login page handles credentials and correctly updates the AuthContext upon successful login.</done>
</task>

</tasks>

<verification>
Ensure the login flow successfully retrieves a token and persists it to local storage.
</verification>

<success_criteria>
- Unauthenticated users see the Login page
- Authenticated users see the App layout
- Login form handles errors gracefully
</success_criteria>

<output>
After completion, create `.planning/phases/03-web-ui/03-02-SUMMARY.md`
</output>
