---
phase: 03-web-ui
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - web-ui-external/index.html
  - web-ui-external/src/auth/AuthContext.tsx
  - web-ui-external/src/pages/Login.tsx
  - web-ui-external/src/App.tsx
  - web-ui-external/src/components/ProtectedRoute.tsx
autonomous: true
requirements:
  - UI-01
  - UI-02
must_haves:
  truths:
    - "Users can log in using Email/Password"
    - "Users can log in using Google OAuth via GIS SDK and backend token exchange"
    - "Login page does not contain any Sign-up or Registration links"
    - "Authentication state persists across page reloads"
    - "Unauthenticated users are redirected to the Login page"
  artifacts:
    - path: "web-ui-external/src/auth/AuthContext.tsx"
      provides: "Authentication state management"
    - path: "web-ui-external/src/pages/Login.tsx"
      provides: "Login UI without registration"
    - path: "web-ui-external/src/components/ProtectedRoute.tsx"
      provides: "Route guard for authenticated areas"
  key_links:
    - from: "web-ui-external/src/pages/Login.tsx"
      to: "Backend Auth API"
      via: "API Client POST to /token and /auth/google"
      pattern: "api.post"
---

<objective>
Implement the authentication flow for the external Web UI, supporting Email/Password login (D-05) and persistent sessions (D-07). Integrates Google OAuth via Google Identity Services (GIS) SDK, performing token exchange with the backend (D-05, removing stub). Enforces no public registration (D-06).

Purpose: Secure the external UI so only authorized users can submit tasks and view results, using only internally provisioned accounts or authorized Google accounts.
Output: Auth context, login page with GIS integration, and protected routing logic.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-web-ui/03-CONTEXT.md
@.planning/phases/03-web-ui/03-RESEARCH.md
@.planning/phases/03-web-ui/03-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup AuthContext and ProtectedRoute</name>
  <files>
    - web-ui-external/src/auth/AuthContext.tsx
    - web-ui-external/src/components/ProtectedRoute.tsx
    - web-ui-external/src/App.tsx
  </files>
  <read_first>
    - web-ui-external/src/api/client.ts
    - web-ui-external/src/App.tsx
  </read_first>
  <action>
    1. Create `web-ui-external/src/auth/AuthContext.tsx` to provide `isAuthenticated`, `user`, `login` (email/pwd), `loginWithGoogle` (for token exchange), and `logout` functions. It should read/write the token to `localStorage` (key: `auth_token`).
    2. Create `web-ui-external/src/components/ProtectedRoute.tsx` that checks `isAuthenticated` from AuthContext. If false, it renders a Login view; if true, it renders its `children`.
    3. Update `web-ui-external/src/App.tsx` to wrap the main application layout in `<AuthProvider>` and `<ProtectedRoute>`.
    4. Ensure successful login automatically redirects the user to the Dashboard per D-02.
  </action>
  <acceptance_criteria>
    - `web-ui-external/src/auth/AuthContext.tsx` contains `createContext` and `localStorage.getItem('auth_token')`
    - `web-ui-external/src/components/ProtectedRoute.tsx` returns children when authenticated
  </acceptance_criteria>
  <verify>
    <automated>cd web-ui-external && npm run build</automated>
  </verify>
  <done>Authentication state is managed globally and protects the main app content.</done>
</task>

<task type="auto">
  <name>Task 2: Build Login UI with GIS Integration</name>
  <files>
    - web-ui-external/index.html
    - web-ui-external/src/pages/Login.tsx
    - web-ui-external/src/components/ProtectedRoute.tsx
  </files>
  <read_first>
    - web-ui-external/src/api/client.ts
    - web-ui-external/src/auth/AuthContext.tsx
  </read_first>
  <action>
    1. Update `web-ui-external/index.html` to include the Google Identity Services SDK (`<script src="https://accounts.google.com/gsi/client" async defer></script>`).
    2. Create `web-ui-external/src/pages/Login.tsx` with a centered card layout containing an Email and Password form.
    3. Use Tailwind classes conforming to UI-SPEC.
    4. Handle form submission by calling the email login function from AuthContext. Include error handling.
    5. Implement Google OAuth login (D-05) using the GIS SDK. Render the Google login button. On successful Google callback, pass the credential (JWT) to AuthContext's `loginWithGoogle` which performs a `POST` to the backend (e.g., `/auth/google`) to exchange for an internal API token.
    6. **CRITICAL:** Do NOT include any "Sign Up", "Register", or "Create Account" links (per D-06). Only "Login" is allowed.
    7. Update `ProtectedRoute.tsx` to render the `<Login />` component (or redirect to `/login`) when unauthenticated.
    8. **API Alignment:** Verify exact Phase 2 auth endpoints (e.g., `/api/auth/token` vs `/token`) before implementation to ensure API contract alignment.
  </action>
  <acceptance_criteria>
    - `web-ui-external/index.html` includes GIS script
    - `web-ui-external/src/pages/Login.tsx` renders GIS button and handles callback
    - `web-ui-external/src/pages/Login.tsx` does not contain registration links
  </acceptance_criteria>
  <verify>
    <automated>cd web-ui-external && npm run build</automated>
  </verify>
  <done>Login page handles email credentials, Google OAuth token exchange, and explicitly omits registration.</done>
</task>

</tasks>

<verification>
Ensure the login flow successfully retrieves a token and persists it to local storage.
</verification>

<success_criteria>
- Unauthenticated users see the Login page
- Login page lacks registration CTA
- Google OAuth is fully integrated with backend token exchange
- Authenticated users see the App layout
- Login form handles errors gracefully
</success_criteria>

<output>
After completion, create `.planning/phases/03-web-ui/03-02-SUMMARY.md`
</output>
