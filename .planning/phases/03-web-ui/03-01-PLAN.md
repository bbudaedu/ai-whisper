---
phase: 03-web-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web-ui-external/package.json
  - web-ui-external/vite.config.ts
  - web-ui-external/tsconfig.json
  - web-ui-external/src/main.tsx
  - web-ui-external/src/App.tsx
  - web-ui-external/src/index.css
  - web-ui-external/src/api/client.ts
autonomous: true
requirements:
  - UI-01
must_haves:
  truths:
    - "Application can be started via standard npm scripts"
    - "Pages render with defined Tailwind CSS utility classes"
    - "API requests successfully reach the backend endpoint"
  artifacts:
    - path: "web-ui-external/package.json"
      provides: "Dependencies for external UI"
    - path: "web-ui-external/src/api/client.ts"
      provides: "Axios instance for backend communication"
    - path: "web-ui-external/src/App.tsx"
      provides: "Root component for external UI"
  key_links:
    - from: "web-ui-external/src/api/client.ts"
      to: "Backend API"
      via: "Axios base configuration"
      pattern: "axios.create"
---

<objective>
Scaffold the independent `web-ui-external` project using the same tech stack as the internal UI (React 19 + Vite + Tailwind v4 + Axios) but with its own structure and API client setup. This establishes the foundation for mobile-first external user interfaces.

Purpose: Isolate the external UI from the internal monitoring dashboard while sharing the same underlying technologies to reduce maintenance overhead.
Output: A running Vite+React app with Tailwind v4 and an API client configured.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-web-ui/03-CONTEXT.md
@.planning/phases/03-web-ui/03-RESEARCH.md
@.planning/phases/03-web-ui/03-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold web-ui-external project</name>
  <files>
    - web-ui-external/package.json
    - web-ui-external/vite.config.ts
    - web-ui-external/tsconfig.json
  </files>
  <read_first>
    - web-ui/package.json
    - web-ui/vite.config.ts
    - web-ui/tsconfig.json
  </read_first>
  <action>
    Create the `web-ui-external` directory and initialize it with the exact same dependencies and configuration as `web-ui`.
    1. Copy `package.json`, `vite.config.ts`, `tsconfig.json`, `tsconfig.node.json`, `eslint.config.js`, and `index.html` from `web-ui` to `web-ui-external`.
    2. In `web-ui-external/package.json`, change the `"name"` field to `"web-ui-external"`.
    3. Ensure `react`, `react-dom`, `vite`, `@tailwindcss/vite`, `tailwindcss`, `axios`, and `lucide-react` are in the dependencies.
    4. Run `npm install` inside `web-ui-external`.
  </action>
  <acceptance_criteria>
    - `web-ui-external/package.json` contains `"name": "web-ui-external"`
    - `web-ui-external/node_modules` exists
  </acceptance_criteria>
  <verify>
    <automated>cd web-ui-external && npm run build</automated>
  </verify>
  <done>Project is successfully created and builds without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Setup basic routing and API client</name>
  <files>
    - web-ui-external/src/api/client.ts
    - web-ui-external/src/main.tsx
    - web-ui-external/src/App.tsx
    - web-ui-external/src/index.css
  </files>
  <read_first>
    - web-ui/src/main.tsx
    - web-ui/src/index.css
  </read_first>
  <action>
    1. Create `web-ui-external/src/index.css` and configure Tailwind v4 by adding `@import "tailwindcss";` and custom color variables for the design system (from UI-SPEC.md: Dominant Light #f9f9fb / Dark #0f1115, Secondary Light #ffffff / Dark #161b22, Accent #3B82F6, Destructive #ef4444).
    2. Create `web-ui-external/src/api/client.ts` to export a pre-configured axios instance: `export const api = axios.create({ baseURL: import.meta.env.VITE_API_BASE_URL || "http://localhost:8002/api" });`. Also setup request interceptors to inject `Authorization: Bearer` token from `localStorage` if it exists.
    3. Setup `web-ui-external/src/main.tsx` to render `App`.
    4. Create `web-ui-external/src/App.tsx` with a basic layout skeleton including a Sidebar placeholder and a Main Content area that responds to mobile views (using Tailwind's `md:flex` classes).
  </action>
  <acceptance_criteria>
    - `web-ui-external/src/api/client.ts` contains `axios.create` and token interceptor logic
    - `web-ui-external/src/index.css` contains `@import "tailwindcss";`
    - `web-ui-external/src/App.tsx` exports the default App component with responsive Tailwind classes (`md:flex`)
  </acceptance_criteria>
  <verify>
    <automated>cd web-ui-external && npm run build</automated>
  </verify>
  <done>Base UI skeleton, styling, and API client are properly structured.</done>
</task>

</tasks>

<verification>
Ensure the new Vite project builds correctly and dependencies resolve.
</verification>

<success_criteria>
- Project `web-ui-external` exists and is installable
- Tailwind v4 styles apply correctly to `App.tsx`
- Axios client handles baseURL and authorization headers globally
</success_criteria>

<output>
After completion, create `.planning/phases/03-web-ui/03-01-SUMMARY.md`
</output>
