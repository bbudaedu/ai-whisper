---
phase: 03-web-ui
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - web-ui-external/src/pages/Dashboard.tsx
  - web-ui-external/src/App.tsx
  - web-ui-external/src/components/Navigation.tsx
autonomous: true
requirements:
  - UI-01
  - UI-06
must_haves:
  truths:
    - "Users land on the Dashboard after login"
    - "Navigation structure follows D-01 (Sidebar + Tab)"
  artifacts:
    - path: "web-ui-external/src/pages/Dashboard.tsx"
      provides: "Main entry view"
    - path: "web-ui-external/src/components/Navigation.tsx"
      provides: "Responsive navigation"
  key_links:
    - from: "web-ui-external/src/App.tsx"
      to: "web-ui-external/src/components/Navigation.tsx"
      via: "Layout composition"
      pattern: "<Navigation />"
---

<objective>
Implement the main navigation structure and Dashboard view for the external UI (D-01, D-02). This provides the structural skeleton where other features (Submit, Track, Settings) will be mounted.

Purpose: Provide a mobile-first layout that easily switches between functional views.
Output: Reusable navigation component and Dashboard placeholder.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-web-ui/03-CONTEXT.md
@.planning/phases/03-web-ui/03-RESEARCH.md
@.planning/phases/03-web-ui/03-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Responsive Navigation</name>
  <files>
    - web-ui-external/src/components/Navigation.tsx
    - web-ui-external/src/App.tsx
  </files>
  <read_first>
    - web-ui/src/App.tsx
    - web-ui-external/src/App.tsx
  </read_first>
  <action>
    1. Create `web-ui-external/src/components/Navigation.tsx` that receives `activeTab` and `setActiveTab` props.
    2. Implement the UI-SPEC constraints for navigation: Left Sidebar for desktop (`md:flex md:flex-col md:w-64`) and Bottom Tab bar for mobile (`md:hidden fixed bottom-0 w-full flex-row`).
    3. Include tabs: Dashboard, 提交任務 (Submit), 任務追蹤 (Track), 播放清單 (Playlists), 設定 (Settings). Use `lucide-react` icons (e.g., LayoutDashboard, PlusCircle, List, PlaySquare, Settings).
    4. Use the Accent color (`#3B82F6` via Tailwind `text-blue-500`) for the active tab.
    5. Update `App.tsx` to include state for `activeTab` and render `<Navigation />` in the layout.
  </action>
  <acceptance_criteria>
    - `web-ui-external/src/components/Navigation.tsx` contains Tailwind classes for both sidebar and bottom bar layouts
    - Active tab uses blue accent color
  </acceptance_criteria>
  <verify>
    <automated>cd web-ui-external && npm run build</automated>
  </verify>
  <done>Navigation bar works across desktop and mobile form factors.</done>
</task>

<task type="auto">
  <name>Task 2: Build Dashboard Overview Page</name>
  <files>
    - web-ui-external/src/pages/Dashboard.tsx
    - web-ui-external/src/App.tsx
  </files>
  <read_first>
    - web-ui-external/src/components/Navigation.tsx
  </read_first>
  <action>
    1. Create `web-ui-external/src/pages/Dashboard.tsx` to serve as the default view.
    2. Add empty state/placeholder content matching UI-SPEC: Heading "尚未建立任何任務", Body "尚未建立任何任務，開始提交音檔或 YouTube 連結。"
    3. Include a primary CTA button "提交任務" that changes the active tab to the submit view.
    4. Wire `Dashboard.tsx` into `App.tsx` so it renders when `activeTab === 'dashboard'`.
  </action>
  <acceptance_criteria>
    - `web-ui-external/src/pages/Dashboard.tsx` contains text "尚未建立任何任務"
    - `web-ui-external/src/App.tsx` conditionally renders `<Dashboard />`
  </acceptance_criteria>
  <verify>
    <automated>cd web-ui-external && npm run build</automated>
  </verify>
  <done>Dashboard renders correctly and offers a path to submit tasks.</done>
</task>

</tasks>

<verification>
Check that the Navigation component renders properly as a sidebar on wide screens and a bottom bar on narrow screens.
</verification>

<success_criteria>
- Mobile and desktop navigation structures exist
- Dashboard is the default view
- Empty states adhere to copy guidelines
</success_criteria>

<output>
After completion, create `.planning/phases/03-web-ui/03-03-SUMMARY.md`
</output>
