---
phase: 03-web-ui
plan: 04
type: execute
wave: 4
depends_on: ["03-03"]
files_modified:
  - web-ui-external/src/pages/SubmitTask.tsx
  - web-ui-external/src/App.tsx
autonomous: true
requirements:
  - UI-03
  - UI-05
  - PROC-01
  - PROC-06
  - PROC-07
must_haves:
  truths:
    - "Users can submit tasks via file upload or YouTube URL"
    - "Users can select audio nature, prompt, and output formats"
  artifacts:
    - path: "web-ui-external/src/pages/SubmitTask.tsx"
      provides: "Task submission form"
  key_links:
    - from: "web-ui-external/src/pages/SubmitTask.tsx"
      to: "Backend API"
      via: "Form submit handler"
      pattern: "api.post.*/api/task"
---

<objective>
Build the Task Submission page, combining file upload and YouTube URL inputs into a single interface (D-09). Provide parameter configuration for audio nature, prompt, and output formats (D-10, D-11).

Purpose: Enable users to submit transcription jobs with specific metadata and output requirements.
Output: A functional submission form connected to the Phase 2 API.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-web-ui/03-CONTEXT.md
@.planning/phases/03-web-ui/03-RESEARCH.md
@.planning/phases/03-web-ui/03-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the Submission Form</name>
  <files>
    - web-ui-external/src/pages/SubmitTask.tsx
    - web-ui-external/src/App.tsx
  </files>
  <read_first>
    - web-ui-external/src/api/client.ts
    - web-ui/src/App.tsx
  </read_first>
  <action>
    1. Create `web-ui-external/src/pages/SubmitTask.tsx`.
    2. Implement a toggle or tab to switch between "Upload File" and "YouTube URL" modes (D-09).
    3. Add input fields for parameters (D-10, D-11):
       - Audio Nature (音檔性質): Dropdown (會議, 佛學課程)
       - Prompt: Textarea with default value.
       - Output Formats: Checkbox group (txt, srt, word, excel, json), all checked by default.
    4. Implement the submit handler. Use `FormData` for file uploads (`multipart/form-data`) and JSON for YouTube URLs. Post to `/api/task` using the configured API client.
    5. Wire `<SubmitTask />` into `App.tsx` for the "submit" tab.
  </action>
  <acceptance_criteria>
    - `web-ui-external/src/pages/SubmitTask.tsx` contains inputs for file, url, prompt, and formats
    - Form submission calls `api.post('/api/task', ...)`
  </acceptance_criteria>
  <verify>
    <automated>cd web-ui-external && npm run build</automated>
  </verify>
  <done>Submission form collects all required parameters and sends them to the API.</done>
</task>

</tasks>

<verification>
Ensure the form state correctly gathers file/URL, prompt, and format checkboxes before POSTing.
</verification>

<success_criteria>
- Submission page handles both file and URL modes
- All parameter fields are present
- API payload structure is valid
</success_criteria>

<output>
After completion, create `.planning/phases/03-web-ui/03-04-SUMMARY.md`
</output>
