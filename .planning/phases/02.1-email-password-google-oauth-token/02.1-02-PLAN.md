---
phase: 02.1-email-password-google-oauth-token
plan: 02
type: execute
wave: 2
depends_on: ["02.1-01"]
files_modified: ["api/schemas.py", "api/auth.py", "pipeline/queue/repository.py"]
autonomous: true
requirements: ["UI-02"]
must_haves:
  truths:
    - 系統支援以 Email/Password 建立使用者與身分
    - 密碼以 Argon2id 加密儲存
    - 提供檢核登入與狀態驗證
  artifacts:
    - path: "api/auth.py"
      provides: "Password hash tool"
      contains: "def hash_password("
    - path: "api/schemas.py"
      provides: "Login request and response schemas"
      contains: "class LoginRequest(BaseModel):"
    - path: "pipeline/queue/repository.py"
      provides: "User and Identity creation"
      contains: "def create_user_with_password("
  key_links:
    - from: "pipeline/queue/repository.py"
      to: "api/models.py"
      via: "User, Identity queries"
      pattern: "from api.models import User, Identity"
---

<objective>
建立 Password hashing (Argon2id) 工具與儲存/驗證的 Repository 方法，以支援 Email/Password 登入流程。
Purpose: 完成登入核心邏輯，準備後續新增 API 端點。
Output: `api/auth.py` 新增 hash/verify 方法，`pipeline/queue/repository.py` 新增使用者存取方法。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@api/auth.py
@api/schemas.py
@pipeline/queue/repository.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Argon2 password hashing</name>
  <files>api/auth.py</files>
  <action>
    在 `api/auth.py` 新增 Password Hashing 功能：
    - 使用 `passlib.context.CryptContext` 包裝 `argon2` 演算法。
    - 定義 `pwd_context = CryptContext(schemes=["argon2"], deprecated="auto")`。
    - 實作 `hash_password(password: str) -> str` 返回 hashed string。
    - 實作 `verify_password(plain_password: str, hashed_password: str) -> bool` 返回驗證結果。
  </action>
  <verify>
    <automated>grep "def hash_password" api/auth.py</automated>
  </verify>
  <acceptance_criteria>
    - `api/auth.py` contains `def hash_password(password: str) -> str:`
    - `api/auth.py` contains `def verify_password(plain_password: str, hashed_password: str) -> bool:`
  </acceptance_criteria>
  <done>Password hashing utilities added</done>
</task>

<task type="auto">
  <name>Task 2: Define auth schemas</name>
  <files>api/schemas.py</files>
  <action>
    在 `api/schemas.py` 新增 `LoginRequest` (email: str, password: str)。
  </action>
  <verify>
    <automated>grep "class LoginRequest(BaseModel):" api/schemas.py</automated>
  </verify>
  <acceptance_criteria>
    - `api/schemas.py` contains `class LoginRequest(BaseModel):`
    - `LoginRequest` has `email: EmailStr` or `email: str`
    - `LoginRequest` has `password: str`
  </acceptance_criteria>
  <done>Auth schemas are defined</done>
</task>

<task type="auto">
  <name>Task 3: Add user repository methods</name>
  <files>pipeline/queue/repository.py</files>
  <action>
    在 `pipeline/queue/repository.py` 新增 `TaskRepository` 方法：
    - 匯入 `User` 與 `Identity` 從 `api.models`。
    - `def get_user_by_email(self, email: str) -> Optional[User]:`
    - `def create_user_with_password(self, email: str, name: str, hashed_password: str, role: str = "external") -> User:` (包含建立 User 與 Identity provider="email")。
    - `def authenticate_user_by_email(self, email: str, password: str) -> Optional[User]:` (查詢 email, 驗證密碼, 確認 `is_active` 是否為 True, 並傳回 `User`。如果密碼錯誤或停用，回傳 `None`。此方法中需呼叫 `api.auth.verify_password`)。
  </action>
  <verify>
    <automated>grep "def get_user_by_email" pipeline/queue/repository.py</automated>
  </verify>
  <acceptance_criteria>
    - `pipeline/queue/repository.py` contains `def get_user_by_email(self, email: str)`
    - `pipeline/queue/repository.py` contains `def create_user_with_password(self, email: str`
    - `pipeline/queue/repository.py` contains `def authenticate_user_by_email(self, email: str, password: str)`
  </acceptance_criteria>
  <done>User access methods implemented in repository</done>
</task>

</tasks>

<verification>
python3 -c "from api.auth import hash_password, verify_password; pwd = hash_password('test1234'); print(verify_password('test1234', pwd))" | grep True
</verification>

<success_criteria>
Password hash 與驗證功能正確，Repository 可進行 User 存取。
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-email-password-google-oauth-token/02.1-02-SUMMARY.md`
</output>
