---
phase: 02.1-email-password-google-oauth-token
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["api/models.py", "pipeline/queue/database.py"]
autonomous: true
requirements: ["UI-02"]
must_haves:
  truths:
    - User/Identity 資料模型可以正確被建立
    - User 包含 active/disabled 狀態
    - Identity 區分 Email 與 Google OAuth
  artifacts:
    - path: "api/models.py"
      provides: "User and Identity database models"
      contains: "class User(SQLModel, table=True):"
    - path: "pipeline/queue/database.py"
      provides: "DB Initialization for new models"
      contains: "from api.models import User, Identity"
  key_links:
    - from: "api/models.py"
      to: "sqlmodel"
      via: "Model definitions"
      pattern: "SQLModel, Field"
---

<objective>
建立 User 與 Identity 資料庫模型，以支援 Phase 02.1 的 Email/Password 與 Google OAuth 登入機制。
Purpose: 讓使用者資料與登入憑證得以持久化儲存，並作為後續實作 Login/OAuth API 的基礎。
Output: `api/models.py` 新增 User, Identity 模型。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@api/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add User and Identity models to models.py</name>
  <files>api/models.py</files>
  <action>
    在 `api/models.py` 新增 `User` 與 `Identity` 模型：
    - `User` 模型包含 `id` (UUID, primary_key=True, default_factory=uuid.uuid4), `email` (str, unique=True, index=True), `name` (str, Optional), `avatar_url` (str, Optional), `is_active` (bool, default=True), `role` (str, default="external"), `created_at` (datetime, default_factory=datetime.utcnow), `updated_at` (datetime, default_factory=datetime.utcnow).
    - `Identity` 模型包含 `id` (int, primary_key=True), `user_id` (UUID, foreign_key="user.id", index=True), `provider` (str, e.g., "email" 或 "google", index=True), `provider_id` (str, 對於 email 則為 email, 對於 google 則為 sub), `hashed_password` (str, Optional, 僅 email/password 登入有值), `created_at` (datetime, default_factory=datetime.utcnow), `updated_at` (datetime, default_factory=datetime.utcnow).
    確保匯入 `uuid` 模組。
    為安全起見，本次 **不修改** `ApiKey` 與 `RefreshToken` 的 `user_id` 欄位型別，保持向後相容。
  </action>
  <verify>
    <automated>grep -E "class User\(SQLModel, table=True\):|class Identity\(SQLModel, table=True\):" api/models.py</automated>
  </verify>
  <acceptance_criteria>
    - `api/models.py` contains `class User(SQLModel, table=True):`
    - `api/models.py` contains `class Identity(SQLModel, table=True):`
    - `User` has `id: uuid.UUID`
    - `Identity` has `user_id: uuid.UUID = Field(foreign_key="user.id")`
  </acceptance_criteria>
  <done>User and Identity models are defined in models.py</done>
</task>

<task type="auto">
  <name>Task 2: Import new models in database.py</name>
  <files>pipeline/queue/database.py</files>
  <action>
    在 `pipeline/queue/database.py` 中，確保 `SQLModel.metadata.create_all` 執行前，`User` 與 `Identity` 模型已被匯入，以便 `create_db_and_tables` 函數能正確建立資料表。
    新增 `from api.models import User, Identity` (如果沒有匯入 `api.models` 的話，或者新增到現有匯入中)。
  </action>
  <verify>
    <automated>grep -E "from api\.models import.*User" pipeline/queue/database.py</automated>
  </verify>
  <acceptance_criteria>
    - `pipeline/queue/database.py` imports `User` and `Identity` from `api.models`
  </acceptance_criteria>
  <done>New models are imported in database.py for schema generation</done>
</task>

</tasks>

<verification>
python3 -c "from pipeline.queue.database import create_db_and_tables; create_db_and_tables()"
</verification>

<success_criteria>
User 與 Identity 資料表成功建立於資料庫中。
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-email-password-google-oauth-token/02.1-01-SUMMARY.md`
</output>
