---
phase: 09-refinement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [pipeline/queue/models.py, api/routers/tasks.py, api/schemas.py, pipeline/queue/repository.py, tests/v2/test_task_update.py]
autonomous: true
requirements: [DB-01, API-01]

must_haves:
  truths:
    - "資料庫 tasks 表包含 speaker_name 欄位"
    - "API 支援 PATCH /api/tasks/{task_id} 更新 speaker_name"
    - "API 回傳的任務詳情包含 speaker_name"
  artifacts:
    - path: "pipeline/queue/models.py"
      provides: "Task model with speaker_name"
    - path: "api/routers/tasks.py"
      provides: "PATCH endpoint implementation"
    - path: "tests/v2/test_task_update.py"
      provides: "API tests for speaker_name update"
  key_links:
    - from: "api/routers/tasks.py"
      to: "pipeline/queue/repository.py"
      via: "session.add(task) and session.commit()"
---

<objective>
擴充後端資料模型與 API 介面，以支援說話者名稱（speaker_name）的儲存與編輯。

Purpose: 為後續 AI 校對提供講者上下文，並允許使用者在 UI 中標註人名。
Output: 具備 speaker_name 欄位的 DB Schema 與對應的 PATCH API 端點。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@pipeline/queue/models.py
@api/routers/tasks.py
@api/schemas.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 更新資料庫模型與執行遷移</name>
  <files>pipeline/queue/models.py</files>
  <action>
    1. 在 `pipeline/queue/models.py` 的 `Task` 類別中新增欄位：
       `speaker_name: Optional[str] = Field(default=None, index=True)` (per DB-01)。
    2. 使用 `sqlite3` 執行手動遷移：`ALTER TABLE tasks ADD COLUMN speaker_name TEXT;`。
       注意：若在測試環境，可能需要刪除舊 DB 或確保測試 Fixture 重新建表。
  </action>
  <verify>
    <automated>sqlite3 database.db "PRAGMA table_info(tasks);" | grep speaker_name</automated>
  </verify>
  <done>資料庫 tasks 表具備 speaker_name 欄位且可索引</done>
</task>

<task type="auto">
  <name>Task 2: 更新 API Schema</name>
  <files>api/schemas.py</files>
  <action>
    1. 在 `TaskStatusResponse` 中新增 `speaker_name: Optional[str] = None`。
    2. 新增 `TaskUpdatePayload(BaseModel)` 包含 `speaker_name: Optional[str] = None`。
  </action>
  <verify>
    <automated>grep "speaker_name" api/schemas.py</automated>
  </verify>
  <done>API Schema 已包含說話者名稱欄位</done>
</task>

<task type="auto">
  <name>Task 3: 建立 API 測試腳本</name>
  <files>tests/v2/test_task_update.py</files>
  <action>
    建立 `tests/v2/test_task_update.py` 並實作測試案例：
    1. 測試 `PATCH /api/tasks/{task_id}` 是否能成功更新 `speaker_name`。
    2. 驗證非法的 `task_id` 回傳 404。
    3. 驗證更新後 `GET /api/tasks/{task_id}` 能取得正確的 `speaker_name`。
  </action>
  <verify>
    <automated>ls tests/v2/test_task_update.py</automated>
  </verify>
  <done>測試腳本已建立且包含必要的測試案例</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: 實作 PATCH /api/tasks/{task_id} 端點</name>
  <files>api/routers/tasks.py</files>
  <action>
    1. 在 `api/routers/tasks.py` 實作 `PATCH` 端點 (per API-01)。
    2. 驗證權限：僅限 Internal 或任務擁有者。
    3. 更新欄位：目前僅支援 `speaker_name`，未來可擴充。
    4. 同步更新 `get_task_status` 的回傳值以包含 `speaker_name`。
  </action>
  <verify>
    <automated>pytest tests/v2/test_task_update.py -v</automated>
  </verify>
  <done>PATCH 端點可正常運作並持久化資料至 DB</done>
</task>

</tasks>

<verification>
1. 檢查資料庫 Schema 是否正確。
2. 執行 pytest 驗證 API 端點（需撰寫測試案例）。
</verification>

<success_criteria>
- 資料庫成功擴充。
- PATCH API 可成功更新 speaker_name。
- GET /api/tasks/{id} 可回傳正確的 speaker_name。
</success_criteria>

<output>
After completion, create `.planning/phases/09-refinement/09-01-SUMMARY.md`
</output>
