---
phase: 09-refinement
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified: [web-ui-external/src/pages/TaskTracker.tsx]
autonomous: false
requirements: [UI-01]

must_haves:
  truths:
    - "使用者可在任務展開列看到說話者人名輸入框"
    - "修改後點擊其他處 (onBlur) 自動儲存"
    - "儲存時顯示載入狀態，完成後顯示更新時間"
  artifacts:
    - path: "web-ui-external/src/pages/TaskTracker.tsx"
      provides: "Inline edit field for speaker_name"
  key_links:
    - from: "web-ui-external/src/pages/TaskTracker.tsx"
      to: "PATCH /api/tasks/{task_id}"
      via: "axios/fetch call in handleUpdateSpeaker"
---

<objective>
在 Web UI 的任務追蹤頁面中實作說話者名稱的編輯功能。

Purpose: 提供直覺的人名標註介面。
Output: 具備 Inline Edit 功能的 TaskTracker 元件。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/09-refinement/09-01-SUMMARY.md
@web-ui-external/src/pages/TaskTracker.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 實作 Inline Edit 欄位</name>
  <files>web-ui-external/src/pages/TaskTracker.tsx</files>
  <action>
    1. 在 `TaskTracker.tsx` 的展開區域 (Expanded Row) 新增一個帶圖示的輸入框 (per UI-01)。
    2. 使用 Lucide 的 `User` 圖示。
    3. 實作內部狀態管理 `editSpeaker`。
    4. 實作 `onBlur` 或「儲存」按鈕觸發 API 調用。
    5. 使用 `client.patch(\`/tasks/\${taskId}\`, { speaker_name: ... })`。
  </action>
  <verify>
    <automated>npm run test:ui (若有 E2E)</automated>
  </verify>
  <done>UI 出現編輯欄位且能與 API 互動</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>TaskTracker 的說話者編輯功能</what-built>
  <how-to-verify>
    1. 啟動 Web UI 與 API。
    2. 展開一個任務。
    3. 在「說話者人名」欄位輸入名稱。
    4. 點擊外部觸發儲存。
    5. 重新整理頁面，確認名稱已持久化。
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
手動視覺檢查編輯狀態與 API 回傳。
</verification>

<success_criteria>
- 說話者欄位顯示正確。
- 儲存流程無報錯且有 UI 回饋。
- 資料跨重新整理持久化。
</success_criteria>

<output>
After completion, create `.planning/phases/09-refinement/09-02-SUMMARY.md`
</output>
