---
phase: 09-refinement
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified: [auto_proofread.py, pipeline/queue/stage_runner.py, tests/v2/test_pipeline_context.py]
autonomous: true
requirements: [LLM-01]

must_haves:
  truths:
    - "AI 校對 Prompt 包含 speaker_name 資訊"
    - "auto_proofread.py 支援從命令行或環境變數讀取 speaker_name"
  artifacts:
    - path: "auto_proofread.py"
      provides: "Enhanced prompt with speaker context"
    - path: "tests/v2/test_pipeline_context.py"
      provides: "Pipeline integration test for speaker_name"
  key_links:
    - from: "pipeline/queue/stage_runner.py"
      to: "auto_proofread.py"
      via: "subprocess call with --speaker_name argument"
---

<objective>
優化 LLM 校對流程，將 `speaker_name` 注入 Prompt Context，並確保串接真實 LLM。

Purpose: 提升 AI 對特定講者名稱的校對精準度。
Output: 支援動態 Context 注入的 auto_proofread.py。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/09-refinement/09-01-SUMMARY.md
@auto_proofread.py
@pipeline/queue/stage_runner.py
</context>

<interfaces>
From auto_proofread.py:
```python
def proofread_chunk(chunk_subtitles, lecture_text, chunk_num, total_chunks, custom_prompt=None):
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 優化 auto_proofread.py 的 Prompt</name>
  <files>auto_proofread.py</files>
  <action>
    1. 在 `proofread_chunk` 函數中新增 `speaker_name` 參數。
    2. 更新 Default Prompt：新增「當前講者：{speaker_name}」資訊。
    3. 更新 ArgumentParser 支援 `--speaker-name`。
  </action>
  <verify>
    <automated>python auto_proofread.py --help | grep speaker-name</automated>
  </verify>
  <done>Prompt 已整合講者資訊</done>
</task>

<task type="auto">
  <name>Task 2: 建立 Pipeline 測試腳本</name>
  <files>tests/v2/test_pipeline_context.py</files>
  <action>
    建立 `tests/v2/test_pipeline_context.py` 並實作測試案例：
    1. 模擬 (Mock) 一個帶有 `speaker_name` 的 `Task` 物件。
    2. 驗證 `ProofreadRunner` 呼叫 `auto_proofread.py` 時是否包含 `--speaker-name` 參數。
    3. 驗證產出的 Prompt 是否包含講者名稱。
  </action>
  <verify>
    <automated>ls tests/v2/test_pipeline_context.py</automated>
  </verify>
  <done>Pipeline 測試腳本已建立</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 更新 Pipeline 呼叫邏輯</name>
  <files>pipeline/queue/stage_runner.py</files>
  <action>
    1. 在 `ProofreadRunner` 中，從 `Task` 模型讀取 `speaker_name`。
    2. 呼叫 `auto_proofread.py` 時傳入 `--speaker-name` 參數。
  </action>
  <verify>
    <automated>pytest tests/v2/test_pipeline_context.py -v</automated>
  </verify>
  <done>Pipeline 成功將 DB 欄位傳遞至校對腳本</done>
</task>

</tasks>

<verification>
執行 Pipeline 冒煙測試，檢查日誌中傳遞給 LLM 的 Prompt 是否包含正確的 speaker_name。
</verification>

<success_criteria>
- AI Prompt 包含正確的講者上下文.
- 校對產出的結果在專有名詞識別上有所改善。
- 串接真實 LLM 運作正常（依賴 config.json 設定）。
</success_criteria>

<output>
After completion, create `.planning/phases/09-refinement/09-03-SUMMARY.md`
</output>
