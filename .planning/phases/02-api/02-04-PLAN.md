---
phase: 02-api
plan: 04
type: execute
wave: 4
depends_on: ["03"]
files_modified:
  - api/schemas.py
  - api/routers/tasks.py
  - pipeline/queue/repository.py
autonomous: true
requirements:
  - API-02
  - API-03
must_haves:
  truths:
    - "User can check the status of their submitted task."
    - "User can cancel their pending task and see it marked canceled."
    - "Cancel response must include reason code."
  artifacts:
    - path: "api/routers/tasks.py"
      provides: "Status retrieval and cancellation endpoints"
    - path: "pipeline/queue/repository.py"
      provides: "Cancellation logic in DB"
  key_links:
    - from: "api_server.py"
      to: "api/routers/tasks.py"
      via: "app.include_router"
      pattern: "app\.include_router"
    - from: "api/routers/tasks.py"
      to: "pipeline/queue/repository.py"
      via: "repository methods"
      pattern: "TaskRepository\."
---

<objective>
Implement API endpoints for querying and canceling tasks, including the necessary repository updates.

Purpose: Allow external users to monitor their task status and cancel them if needed, fulfilling API-02 and API-03.
Output: `api/routers/tasks.py` updated with GET and PUT/POST endpoints, `pipeline/queue/repository.py` updated for cancellation returning reason codes.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@pipeline/queue/repository.py
@api/routers/tasks.py
@pipeline/queue/models.py
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add get and cancel methods to repository</name>
  <files>pipeline/queue/repository.py</files>
  <read_first>
    - pipeline/queue/repository.py
  </read_first>
  <action>
    Add querying and cancellation methods to `pipeline/queue/repository.py`:
    1. Update `get_task`: Add `requester: Optional[str] = None` to filter.
    2. Update `get_tasks`: Add `requester` and `status` filters.
    3. Add `cancel_task(self, task_id: int, requester: str, role: str) -> dict`:
       - Check if task exists. Check if `requester == task.requester` or `role == "internal"`.
       - If invalid permissions, return `{"status": "error", "reason": "unauthorized"}` (or raise appropriate exception caught by router).
       - If `task.status not in [TaskStatus.PENDING, TaskStatus.QUEUED]`, return `{"status": "unchanged", "reason": "task_already_running"}` (or similar descriptive reason code).
       - If valid, update `task.status = TaskStatus.CANCELED`, `task.error_message = "client_cancel"`. Update all `StageTask` for this `task_id` that are pending/queued to canceled.
       - Return `{"status": "canceled", "reason": "client_cancel"}`.
  </action>
  <verify>
    <automated>grep -q "def cancel_task" pipeline/queue/repository.py && grep -q "requester: Optional\[str\]" pipeline/queue/repository.py</automated>
  </verify>
  <acceptance_criteria>
    - `pipeline/queue/repository.py` contains `def cancel_task` returning status/reason dict or raising specific exceptions
    - `pipeline/queue/repository.py` contains `task.status = TaskStatus.CANCELED`
    - `pipeline/queue/repository.py`'s `get_task` takes `requester` parameter
  </acceptance_criteria>
  <done>Repository is ready for queries and cancellations with reason codes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add status and cancel endpoints</name>
  <files>api/routers/tasks.py, api/schemas.py</files>
  <read_first>
    - api/routers/tasks.py
    - api/schemas.py
    - pipeline/queue/models.py
  </read_first>
  <action>
    Add API endpoints to `api/routers/tasks.py`:
    1. Add schema `TaskStatusResponse(BaseModel): id: int; title: str; status: TaskStatus; created_at: datetime; requester: str` to `api/schemas.py`.
    2. Add schema `TaskCancelResponse(BaseModel): status: str; reason: str` to `api/schemas.py`.
    3. Add `@router.get("/{task_id}", response_model=TaskStatusResponse)` that fetches from `TaskRepository`. Check `requester` and `role`.
    4. Add `@router.post("/{task_id}/cancel", response_model=TaskCancelResponse)` that calls `TaskRepository.cancel_task`.
       - Per D-20, return 200 OK with state/reason even when unable to cancel.
       - Parse the return from `cancel_task` and ensure the response is always a 200 OK containing `{status, reason}` on non-cancelable cases (e.g., status="unchanged", reason="task_already_running").
       - Per D-16, set reason=client_cancel on success. Per D-17, cancel synchronously. Ensure D-18 (do not delete files) is respected.
  </action>
  <verify>
    <automated>grep -q "@router.get(\"/{task_id}\"" api/routers/tasks.py && grep -q "@router.post(\"/{task_id}/cancel\")" api/routers/tasks.py && grep -q "TaskCancelResponse" api/schemas.py</automated>
  </verify>
  <acceptance_criteria>
    - `api/routers/tasks.py` contains `@router.get("/{task_id}")`
    - `api/routers/tasks.py` contains `@router.post("/{task_id}/cancel")`
    - `api/schemas.py` contains `TaskStatusResponse` and `TaskCancelResponse`
    - Cancel endpoint returns 200 OK with reason code for both success and failure cases
  </acceptance_criteria>
  <done>Querying and cancellation endpoints are fully functional and return required reason codes.</done>
</task>

</tasks>

<verification>
- Verify `TaskRepository` methods correctly filter and cancel returning status info.
- Verify `api/routers/tasks.py` endpoints are present and cancel returns reason code.
</verification>

<success_criteria>
External users can query and cancel their own tasks via API, with explicit reason codes provided.
</success_criteria>

<output>
After completion, create `.planning/phases/02-api/02-04-SUMMARY.md`
</output>