---
phase: 02-api
plan: 04
type: execute
wave: 4
depends_on: ["03"]
files_modified:
  - api/schemas.py
  - api/routers/tasks.py
  - pipeline/queue/repository.py
autonomous: true
requirements:
  - API-02
  - API-03
must_haves:
  truths:
    - "Task status can be retrieved via API."
    - "Task can be canceled via API."
  artifacts:
    - path: "api/routers/tasks.py"
      provides: "Status retrieval and cancellation endpoints"
    - path: "pipeline/queue/repository.py"
      provides: "Cancellation logic in DB"
  key_links: []
---

<objective>
Implement API endpoints for querying and canceling tasks, including the necessary repository updates.

Purpose: Allow external users to monitor their task status and cancel them if needed, fulfilling API-02 and API-03.
Output: `api/routers/tasks.py` updated with GET and PUT/POST endpoints, `pipeline/queue/repository.py` updated for cancellation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@pipeline/queue/repository.py
@api/routers/tasks.py
@pipeline/queue/models.py
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add get and cancel methods to repository</name>
  <read_first>
    - pipeline/queue/repository.py
  </read_first>
  <action>
    Add querying and cancellation methods to `pipeline/queue/repository.py`:
    1. Update `get_task`: Add `requester: Optional[str] = None` to filter.
    2. Update `get_tasks`: Add `requester` and `status` filters.
    3. Add `cancel_task(self, task_id: int, requester: str, role: str) -> bool`:
       - Check if `task.status in [TaskStatus.PENDING, TaskStatus.QUEUED]`.
       - Check if `requester == task.requester` or `role == "internal"`.
       - If valid, update `task.status = TaskStatus.CANCELED`, `task.error_message = "client_cancel"`. Update all `StageTask` for this `task_id` that are pending/queued to canceled.
  </action>
  <verify>
    <automated>grep -q "def cancel_task" pipeline/queue/repository.py && grep -q "requester: Optional\[str\]" pipeline/queue/repository.py</automated>
  </verify>
  <acceptance_criteria>
    - `pipeline/queue/repository.py` contains `def cancel_task`
    - `pipeline/queue/repository.py` contains `task.status = TaskStatus.CANCELED`
    - `pipeline/queue/repository.py`'s `get_task` takes `requester` parameter
  </acceptance_criteria>
  <done>Repository is ready for queries and cancellations.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add status and cancel endpoints</name>
  <read_first>
    - api/routers/tasks.py
    - api/schemas.py
    - pipeline/queue/models.py
  </read_first>
  <action>
    Add API endpoints to `api/routers/tasks.py`:
    1. Add schema `TaskStatusResponse(BaseModel): id: int; title: str; status: TaskStatus; created_at: datetime; requester: str` to `api/schemas.py`.
    2. Add `@router.get("/{task_id}", response_model=TaskStatusResponse)` that fetches from `TaskRepository`. Check `requester` and `role`.
    3. Add `@router.post("/{task_id}/cancel")` that calls `TaskRepository.cancel_task`. Return `{ "status": "canceled", "reason": "client_cancel" }` on success, `404` if not found, `403` if unauthorized, `400` if not in a cancelable state.
  </action>
  <verify>
    <automated>grep -q "@router.get(\"/{task_id}\"" api/routers/tasks.py && grep -q "@router.post(\"/{task_id}/cancel\")" api/routers/tasks.py</automated>
  </verify>
  <acceptance_criteria>
    - `api/routers/tasks.py` contains `@router.get("/{task_id}")`
    - `api/routers/tasks.py` contains `@router.post("/{task_id}/cancel")`
    - `api/schemas.py` contains `TaskStatusResponse`
  </acceptance_criteria>
  <done>Querying and cancellation endpoints are fully functional.</done>
</task>

</tasks>

<verification>
- Verify `TaskRepository` methods correctly filter and cancel.
- Verify `api/routers/tasks.py` endpoints are present.
</verification>

<success_criteria>
External users can query and cancel their own tasks via API.
</success_criteria>

<output>
After completion, create `.planning/phases/02-api/02-04-SUMMARY.md`
</output>