---
phase: 02-api
plan: 06
type: execute
wave: 6
depends_on: ["05"]
files_modified: []
autonomous: false
requirements:
  - API-01
  - API-02
  - API-03
  - API-04
  - API-05
must_haves:
  truths:
    - "Human can verify API endpoints function correctly with correct auth headers."
  artifacts: []
  key_links: []
---

<objective>
Manually verify the completed Phase 02 endpoints using an HTTP client (curl, Postman, etc.).

Purpose: Ensure the end-to-end user experience matches the requirements before closing the phase.
Output: Verification and User Acceptance.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human verify API workflow</name>
  <files></files>
  <read_first></read_first>
  <action>Pause for human verification.</action>
  <verify><automated>echo "Verified"</automated></verify>
  <what-built>API authentication, task submission, status checks, cancellation, and download endpoints.</what-built>
  <how-to-verify>
    1. Start the API server: `python3 -m uvicorn api_server:app --reload`
    2. Get a token using API key `test-api-key-123`: `curl -X POST "http://localhost:8000/api/auth/token" -H "x-api-key: test-api-key-123"`
    3. Use the token to create a YouTube task: `curl -X POST "http://localhost:8000/api/tasks" -H "Authorization: Bearer <TOKEN>" -H "Content-Type: application/json" -d '{"type": "youtube", "source": "api", "payload": "{\"url\":\"https://youtube.com/watch?v=123\"}"}'`
    4. Check task status: `curl -X GET "http://localhost:8000/api/tasks/<task_id>" -H "Authorization: Bearer <TOKEN>"`
    5. Cancel task: `curl -X POST "http://localhost:8000/api/tasks/<task_id>/cancel" -H "Authorization: Bearer <TOKEN>"`
    6. Try to download task (should fail if canceled, succeed if done): `curl -X GET "http://localhost:8000/api/tasks/<task_id>/download" -H "Authorization: Bearer <TOKEN>"`
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
  <acceptance_criteria>
    - Human approves the API workflow
  </acceptance_criteria>
  <done>Phase 02 API endpoints are verified by a human.</done>
</task>

</tasks>

<verification>
- Verify human acceptance of the workflow.
</verification>

<success_criteria>
All Phase 02 API functionality works as intended.
</success_criteria>

<output>
After completion, create `.planning/phases/02-api/02-06-SUMMARY.md`
</output>