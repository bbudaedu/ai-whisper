---
phase: 02-api
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - api/schemas.py
  - api/auth.py
  - api_server.py
autonomous: true
requirements:
  - API-05
must_haves:
  truths:
    - "JWT token generation via API key exchange works."
    - "Auth module can validate Bearer tokens."
  artifacts:
    - path: "api/auth.py"
      provides: "JWT generation and validation"
    - path: "api/schemas.py"
      provides: "Pydantic models for auth requests/responses"
    - path: "api_server.py"
      provides: "FastAPI app instance and API key router"
  key_links: []
---

<objective>
Implement API Key exchange for JWT authentication as per D-01 through D-07.

Purpose: Provide the foundational authentication mechanism for all external API endpoints.
Output: `api/auth.py` and `api/schemas.py` created, and `api_server.py` updated with the auth route.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@api_server.py
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create auth schemas and security dependencies</name>
  <files>
  </files>
  <read_first>
    - api_server.py
  </read_first>
  <action>
    Create the necessary files and schemas for authentication:
    1. Create `api/schemas.py`. Add Pydantic models:
       - `class Token(BaseModel): access_token: str; token_type: str = "bearer"`
    2. Create `api/auth.py`. Implement JWT generation and validation using `PyJWT`.
       - `def create_access_token(data: dict, expires_delta: timedelta | None = None)`
       - `def verify_token(token: str) -> dict`
       - Use `fastapi.security.HTTPBearer` and `fastapi.security.APIKeyHeader(name="x-api-key")` dependencies.
       - The token should contain `user_id`, `role`, and `exp` claims.
  </action>
  <verify>
    <automated>grep -q "create_access_token" api/auth.py && grep -q "verify_token" api/auth.py</automated>
  </verify>
  <acceptance_criteria>
    - `api/schemas.py` exists and contains `class Token(BaseModel)`
    - `api/auth.py` exists and contains `create_access_token` and `verify_token`
    - `api/auth.py` imports `HTTPBearer` and `APIKeyHeader` from `fastapi.security`
  </acceptance_criteria>
  <done>Auth utilities and schemas are created.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Integrate auth endpoints into API server</name>
  <files>
  </files>
  <read_first>
    - api_server.py
    - api/auth.py
    - api/schemas.py
  </read_first>
  <action>
    Update `api_server.py` to include the new authentication route:
    1. Import `APIKeyHeader` from `fastapi.security` and `create_access_token` from `api.auth`.
    2. Add an endpoint `POST /api/auth/token` that takes an API key via header (`x-api-key`) and returns a JWT token.
       - Note: For Phase 2 MVP, accept a hardcoded API key (e.g., `test-api-key-123`) mapping to `user_id="test_user", role="external"` and another for `internal`.
  </action>
  <verify>
    <automated>grep -q "@app.post(\"/api/auth/token\")" api_server.py</automated>
  </verify>
  <acceptance_criteria>
    - `api_server.py` contains `@app.post("/api/auth/token")` route
    - `api_server.py` uses `x-api-key` header for authentication
    - The endpoint returns a JSON containing `access_token`
  </acceptance_criteria>
  <done>Auth endpoint integrated into main API server.</done>
</task>

</tasks>

<verification>
- Verify `api/schemas.py` contains `Token` model.
- Verify `api/auth.py` handles PyJWT logic.
- Verify `api_server.py` exposes `/api/auth/token`.
</verification>

<success_criteria>
Authentication logic implemented and integrated into API server.
</success_criteria>

<output>
After completion, create `.planning/phases/02-api/02-02-SUMMARY.md`
</output>