---
phase: 02-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pipeline/queue/models.py
  - tests/test_external_api_auth.py
  - tests/test_external_api_tasks.py
  - tests/test_external_api_download.py
autonomous: true
requirements:
  - API-01
  - API-02
  - API-03
  - API-04
  - API-05
must_haves:
  truths:
    - "Test files for external API validation exist and can be run."
    - "Data models have required fields (requester, queued/canceled states) to support API tasks."
  artifacts:
    - path: "pipeline/queue/models.py"
      provides: "Updated Task and TaskStatus models"
    - path: "tests/test_external_api_auth.py"
      provides: "Test stubs for API-05"
    - path: "tests/test_external_api_tasks.py"
      provides: "Test stubs for API-01, API-02, API-03"
    - path: "tests/test_external_api_download.py"
      provides: "Test stubs for API-04"
  key_links: []
---

<objective>
Update queue models to support API functionality (auth, user context, new statuses) and create test stubs for the upcoming implementation wave.

Purpose: Set the data foundation for external API routes to be built upon and fulfill Wave 0 verification requirements.
Output: Updated `models.py` with `requester` and new `TaskStatus` values, along with 3 new test files containing failing tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@pipeline/queue/models.py
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update queue models for external API support</name>
  <files>
  </files>
  <read_first>
    - pipeline/queue/models.py
  </read_first>
  <action>
    Modify `pipeline/queue/models.py` to support external API requirements:
    1. Update `TaskStatus` enum to include new states required by D-13: Add `QUEUED = "queued"` and `CANCELED = "canceled"`.
    2. Update `Task` model: Add `requester: Optional[str] = Field(default=None, index=True)` to track which user created the task (to support D-06).
  </action>
  <verify>
    <automated>grep -q "QUEUED = \"queued\"" pipeline/queue/models.py && grep -q "CANCELED = \"canceled\"" pipeline/queue/models.py && grep -q "requester: Optional\[str\]" pipeline/queue/models.py</automated>
  </verify>
  <acceptance_criteria>
    - `pipeline/queue/models.py` contains `QUEUED = "queued"`
    - `pipeline/queue/models.py` contains `CANCELED = "canceled"`
    - `pipeline/queue/models.py` contains `requester: Optional[str] = Field(default=None, index=True)`
  </acceptance_criteria>
  <done>Models updated with required states and fields for external API.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create Wave 0 Test Stubs</name>
  <files>
  </files>
  <read_first>
    - tests/conftest.py
  </read_first>
  <action>
    Create test stubs for the external API endpoints to satisfy Wave 0 requirements. These tests should be empty or contain `pass` for now (or a simple failing assert like `assert False, "Not implemented yet"` to ensure they are picked up and fail).

    1. Create `tests/test_external_api_auth.py` with `def test_api_key_exchange(): assert False, "Not implemented"`
    2. Create `tests/test_external_api_tasks.py` with:
       - `def test_create_task(): assert False, "Not implemented"`
       - `def test_get_status(): assert False, "Not implemented"`
       - `def test_cancel_task(): assert False, "Not implemented"`
    3. Create `tests/test_external_api_download.py` with `def test_download_zip(): assert False, "Not implemented"`
  </action>
  <verify>
    <automated>ls tests/test_external_api_auth.py tests/test_external_api_tasks.py tests/test_external_api_download.py</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_external_api_auth.py` exists and contains `test_api_key_exchange`
    - `tests/test_external_api_tasks.py` exists and contains `test_create_task`, `test_get_status`, `test_cancel_task`
    - `tests/test_external_api_download.py` exists and contains `test_download_zip`
  </acceptance_criteria>
  <done>Wave 0 test stubs created and verified to exist.</done>
</task>

</tasks>

<verification>
- Verify `TaskStatus` includes `queued` and `canceled`.
- Verify `Task` model has `requester` field.
- Verify 3 new test files exist in `tests/`.
</verification>

<success_criteria>
Models are ready for API integration and Wave 0 tests are created.
</success_criteria>

<output>
After completion, create `.planning/phases/02-api/02-01-SUMMARY.md`
</output>