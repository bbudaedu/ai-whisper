---
phase: 02-api
plan: 03
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - api/schemas.py
  - api/routers/tasks.py
  - api_server.py
autonomous: true
requirements:
  - API-01
must_haves:
  truths:
    - "User can submit a YouTube task via single endpoint."
    - "User can upload an audio file via single endpoint."
    - "Response contains task ID and initial status."
  artifacts:
    - path: "api/routers/tasks.py"
      provides: "Task submission API endpoint"
    - path: "api/schemas.py"
      provides: "Pydantic models for task creation requests"
  key_links:
    - from: "api_server.py"
      to: "api/routers/tasks.py"
      via: "app.include_router"
      pattern: "app\.include_router"
    - from: "api/routers/tasks.py"
      to: "pipeline/queue/repository.py"
      via: "repository methods"
      pattern: "TaskRepository\."
---

<objective>
Implement the single task creation API endpoint supporting both upload and youtube types.

Purpose: Allow external users to submit audio or YouTube links for processing via a unified endpoint, fulfilling API-01 and user decisions D-08 through D-14. Resolve encoding conflict.
Output: `api/routers/tasks.py` created with single `POST /api/tasks` endpoint, schemas updated, and router mounted in `api_server.py`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@api_server.py
@pipeline/queue/models.py
@pipeline/queue/repository.py
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add unified task submission schemas and endpoint</name>
  <files>
    api/schemas.py, api/routers/tasks.py
  </files>
  <read_first>
    - api/schemas.py
    - pipeline/queue/models.py
  </read_first>
  <action>
    Add task creation schemas and endpoint respecting D-08, D-09, D-11, D-12, and D-14:
    1. Create `api/routers/tasks.py`. Create `router = APIRouter(prefix="/api/tasks", tags=["Tasks"])`
    2. Import `HTTPBearer` security dependency. `async def get_current_user(token: HTTPAuthorizationCredentials = Depends(HTTPBearer()))`
    3. Add schemas to `api/schemas.py`:
       - `class TaskCreateResponse(BaseModel): task_id: int; status: str; created_at: datetime` (per D-10)
    4. Implement single `POST /` (mounted at `/api/tasks`) in `api/routers/tasks.py` (per D-08).
       - To support both `multipart/form-data` (upload) and `application/json` (youtube) in a single endpoint in FastAPI without schema conflict:
         - Inspect the `Request` content-type header: `request: Request`.
         - If `application/json`, parse body as JSON: `body = await request.json()`, extract `type`, `source`, `payload`, `output_formats`.
         - If `multipart/form-data`, parse form: `form = await request.form()`, extract fields and `file: UploadFile`.
         - Return 400 for unsupported content types.
       - Parameters extracted:
         - `type: str` (must be 'upload' or 'youtube')
         - `source: str` (default "api")
         - `payload: str` or `dict` (for youtube, should contain url. for upload, could be empty or prompt)
         - `file: UploadFile` (only for multipart/upload)
         - `output_formats: str` or `list` (default ["srt", "vtt", "txt"])
       - Logic:
         - Validate `type`. If 'upload', `file` must be present. If 'youtube', `payload` must contain `url` (per D-12).
         - Parse `payload` and `output_formats` properly if they come as strings from form data.
         - Construct task and save using `TaskRepository.create_task`.
         - Inject `requester` from the decoded JWT token.
  </action>
  <verify>
    <automated>grep -q "@router.post(\"/\")" api/routers/tasks.py || grep -q "@router.post(\"\")" api/routers/tasks.py</automated>
  </verify>
  <acceptance_criteria>
    - `api/routers/tasks.py` exists and contains a single POST endpoint for task creation
    - Endpoint inspects `content-type` to handle both `application/json` and `multipart/form-data`
    - Endpoint validates `type` (upload/youtube)
    - Endpoint parses `payload` and `output_formats`
    - Endpoint handles optional `file` for upload type
    - Endpoint returns `TaskCreateResponse`
  </acceptance_criteria>
  <done>Unified task submission endpoint created and secured.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Mount tasks router</name>
  <files>api_server.py</files>
  <read_first>
    - api_server.py
    - api/routers/tasks.py
  </read_first>
  <action>
    Update `api_server.py` to include the tasks router:
    1. Import `router` from `api.routers.tasks` as `tasks_router`.
    2. Mount the router using `app.include_router(tasks_router)`.
  </action>
  <verify>
    <automated>grep -q "app.include_router(tasks_router" api_server.py</automated>
  </verify>
  <acceptance_criteria>
    - `api_server.py` contains `from api.routers.tasks import router as tasks_router`
    - `api_server.py` contains `app.include_router(tasks_router)`
  </acceptance_criteria>
  <done>Tasks router is integrated into main API server.</done>
</task>

</tasks>

<verification>
- Verify `api/routers/tasks.py` contains a single endpoint handling both types via content-type inspection.
- Verify `api_server.py` mounts the router.
</verification>

<success_criteria>
Task submission endpoint is fully functional, follows unified design, handles encoding properly, and is integrated.
</success_criteria>

<output>
After completion, create `.planning/phases/02-api/02-03-SUMMARY.md`
</output>