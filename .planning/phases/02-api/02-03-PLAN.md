---
phase: 02-api
plan: 03
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - api/schemas.py
  - api/routers/tasks.py
  - api_server.py
autonomous: true
requirements:
  - API-01
must_haves:
  truths:
    - "Task submission API endpoint is available and secured."
    - "Supports 'upload' (multipart) and 'youtube' (JSON) submission."
  artifacts:
    - path: "api/routers/tasks.py"
      provides: "Task submission API endpoint"
    - path: "api/schemas.py"
      provides: "Pydantic models for task creation requests"
  key_links: []
---

<objective>
Implement the task creation API endpoint supporting multipart upload and JSON payloads.

Purpose: Allow external users to submit audio or YouTube links for processing, fulfilling API-01.
Output: `api/routers/tasks.py` created with `POST /api/tasks` endpoint, schemas updated, and router mounted in `api_server.py`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@api_server.py
@pipeline/queue/models.py
@pipeline/queue/repository.py
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add task submission schemas and endpoint</name>
  <read_first>
    - api/schemas.py
    - pipeline/queue/models.py
  </read_first>
  <action>
    Add task creation schemas and endpoint:
    1. Create `api/routers/tasks.py`. Create `router = APIRouter(prefix="/api/tasks", tags=["Tasks"])`
    2. Import `HTTPBearer` security dependency. `async def get_current_user(token: HTTPAuthorizationCredentials = Depends(HTTPBearer()))`
    3. Add schemas to `api/schemas.py`:
       - `class TaskCreateResponse(BaseModel): task_id: int; status: str; created_at: datetime`
    4. Implement `POST /api/tasks` in `api/routers/tasks.py`.
       - Accept `type: str = Form(...)` or `type: str = Body(...)`. Use a wrapper function or `fastapi.Request` if needed to handle both JSON and multipart, or create two separate endpoints (e.g., `/api/tasks/upload` and `/api/tasks/youtube`) for simplicity, mapping to D-08's intent of a unified entry. Given FastAPI constraints (JSON + File is hard), we will create a single endpoint that uses Form fields for *all* data if a file is present (`type=upload`), and JSON if no file is present (`type=youtube`), OR we use separate endpoints. Let's use **separate endpoints** for clarity:
         - `POST /api/tasks/upload`: `file: UploadFile = File(...)`, `prompt: str = Form(default="")`
         - `POST /api/tasks/youtube`: `class YoutubeTaskCreate(BaseModel): url: str; playlist_id: Optional[str] = None; prompt: str = ""`
       - Both endpoints should use `TaskRepository.create_task` and `create_initial_stages`.
       - Inject `requester` from the decoded JWT token (`get_current_user`).
  </action>
  <verify>
    <automated>grep -q "@router.post(\"/upload\")" api/routers/tasks.py && grep -q "@router.post(\"/youtube\")" api/routers/tasks.py</automated>
  </verify>
  <acceptance_criteria>
    - `api/routers/tasks.py` exists and contains `@router.post("/upload")` and `@router.post("/youtube")`
    - Both endpoints depend on `HTTPBearer`
    - Both endpoints use `TaskRepository.create_task`
    - Both endpoints return `TaskCreateResponse`
  </acceptance_criteria>
  <done>Task submission endpoints created and secured.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Mount tasks router</name>
  <read_first>
    - api_server.py
    - api/routers/tasks.py
  </read_first>
  <action>
    Update `api_server.py` to include the tasks router:
    1. Import `router` from `api.routers.tasks` as `tasks_router`.
    2. Mount the router using `app.include_router(tasks_router)`.
  </action>
  <verify>
    <automated>grep -q "app.include_router(tasks_router" api_server.py</automated>
  </verify>
  <acceptance_criteria>
    - `api_server.py` contains `from api.routers.tasks import router as tasks_router`
    - `api_server.py` contains `app.include_router(tasks_router)`
  </acceptance_criteria>
  <done>Tasks router is integrated into main API server.</done>
</task>

</tasks>

<verification>
- Verify `api/routers/tasks.py` contains endpoints for upload and youtube.
- Verify `api_server.py` mounts the router.
</verification>

<success_criteria>
Task submission endpoints are fully functional and integrated.
</success_criteria>

<output>
After completion, create `.planning/phases/02-api/02-03-SUMMARY.md`
</output>