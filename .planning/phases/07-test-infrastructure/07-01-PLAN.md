---
phase: 07-test-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [tests/v2/conftest.py, tests/v2/test_config.v2.json, tests/fixtures/v2/sample.wav]
autonomous: true
requirements: [TEST-04]
must_haves:
  truths:
    - "測試執行時不會修改或產生實體 .db 檔案"
    - "測試環境會自動讀取測試專用的 config 並停用真實外部服務 (Webhook/Mail)"
  artifacts:
    - path: "tests/v2/conftest.py"
      provides: "Pytest fixtures for DB isolation and config patching"
    - path: "tests/v2/test_config.v2.json"
      provides: "Sandboxed configuration"
  key_links:
    - from: "tests/v2/conftest.py"
      to: "api/database.py"
      via: "SQLAlchemy engine override to sqlite:///:memory:"
---

<objective>
建立 v2.0 測試基礎設施，確保測試環境與正式環境完全隔離，並準備測試所需的資產檔案。

Purpose: 避免測試污染正式資料庫，並提供一致的模擬配置。
Output: 具備自動隔離功能的 pytest 環境與測試資產。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/07-test-infrastructure/07-CONTEXT.md
@api/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 建立測試資產與沙盒設定檔</name>
  <files>tests/v2/test_config.v2.json, tests/fixtures/v2/sample.wav</files>
  <action>
    1. 建立 `tests/v2/test_config.v2.json`，內容包含：
       - `database_url`: "sqlite:///:memory:"
       - `enable_webhook`: false
       - `enable_mail`: false
       - `whisper_model`: "tiny" (用於 Smoke test)
       - `llm_provider`: "mock"
    2. 建立 `tests/fixtures/v2/` 目錄，並放置一個極短的 sample.wav (可從現有 fixtures 複製)。
  </action>
  <verify>ls tests/v2/test_config.v2.json tests/fixtures/v2/sample.wav</verify>
  <done>測試資產與設定檔就緒</done>
</task>

<task type="auto">
  <name>Task 2: 實作全域 conftest.py 與環境隔離</name>
  <files>tests/v2/conftest.py</files>
  <action>
    實作 pytest fixtures：
    1. `test_settings`: 使用 `unittest.mock.patch` 強制 `api.config.settings` 載入 `test_config.v2.json`。
    2. `db_session`: 使用 `SQLAlchemy` 建立 `sqlite:///:memory:` 連線，並在每個測試開始前執行 `Base.metadata.create_all`，結束後 `drop_all`。
    3. `client`: 建立 `fastapi.testclient.TestClient`，並注入 `db_session` 覆蓋 FastAPI 的 `get_db` dependency。
  </action>
  <verify>pytest tests/v2 -k "test_isolation" (需先寫一個簡單的 assertion 驗證 DB 是空的)</verify>
  <done>所有測試皆在記憶體資料庫中執行，且 config 已成功切換</done>
</task>

</tasks>

<verification>
執行 `pytest tests/v2/conftest.py` 確保無語法錯誤且環境初始化正確。
</verification>

<success_criteria>
- 執行測試後，專案根目錄未產生新的 .db 檔案。
- 測試環境中的 settings.enable_webhook 為 false。
</success_criteria>

<output>
After completion, create `.planning/phases/07-test-infrastructure/07-01-SUMMARY.md`
</output>
