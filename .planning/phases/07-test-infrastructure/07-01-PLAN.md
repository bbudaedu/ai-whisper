---
phase: 07-test-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/v2/__init__.py
  - tests/v2/conftest.py
  - tests/v2/test_config.v2.json
  - tests/v2/test_isolation.py
autonomous: true
requirements: [TEST-04]
must_haves:
  truths:
    - "tests/v2/ 下的測試執行時不會在專案目錄產生任何 .db 檔案"
    - "tests/v2/conftest.py 的 client fixture 包含 auth + tasks + download 三個 router"
    - "tests/v2/conftest.py 繼承父層 tests/conftest.py 的 db_engine/db_session fixtures，不重複定義"
    - "test_isolation.py 驗證 DB 隔離與 fixture 正確性"
  artifacts:
    - path: "tests/v2/conftest.py"
      provides: "client, auth_header, internal_auth_header, patched get_session/OUTPUT_BASE/log_task_event"
    - path: "tests/v2/test_config.v2.json"
      provides: "Semantic test config with database_url, enable_webhook=false, jwt_secret"
    - path: "tests/v2/test_isolation.py"
      provides: "Smoke test verifying DB isolation and fixture wiring"
  key_links:
    - from: "tests/v2/conftest.py"
      to: "pipeline/queue/database.py"
      via: "monkeypatch override of get_session to use inherited db_engine"
    - from: "tests/v2/conftest.py"
      to: "api/auth.py"
      via: "monkeypatch JWT_SECRET and create_access_token for auth_header fixtures"
---

<objective>
建立 v2.0 測試基礎設施：conftest.py（繼承父層 db_engine）、test_config.v2.json（語義配置）、驗證隔離的 test_isolation.py。

Purpose: 提供所有後續 07-02/07-03 測試的基礎 fixtures，確保 DB、OUTPUT_BASE、log_task_event 三大副作用被完全攔截。
Output: `pytest tests/v2/test_isolation.py -v` 全部通過。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/07-test-infrastructure/07-RESEARCH.md
@tests/conftest.py
@tests/test_external_api_auth.py
@api/auth.py
@api/routers/tasks.py
@api/routers/download.py
@pipeline/queue/database.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 建立目錄結構與語義設定檔</name>
  <files>tests/v2/__init__.py, tests/v2/test_config.v2.json</files>
  <read_first>
    - tests/conftest.py（確認父層 fixtures 命名與可繼承性）
    - tests/fixtures/test_audio.wav（確認存在，可複製為 v2 測試資產）
  </read_first>
  <action>
    1. 建立 `tests/v2/__init__.py`（空檔案，讓 pytest 識別為 package）。

    2. 建立 `tests/v2/test_config.v2.json`，內容如下：
       ```json
       {
         "database_url": "sqlite:///:memory:",
         "enable_webhook": false,
         "enable_mail": false,
         "whisper_model": "tiny",
         "llm_provider": "mock",
         "jwt_secret": "test-secret-v2"
       }
       ```
       注意：此檔案為語義記錄，實際注入靠 conftest.py 的 monkeypatch。
       程式碼中 `api.auth.JWT_SECRET` 讀取順序是 env `JWT_SECRET` → `config.json.jwt_secret`，
       測試中需用 monkeypatch 直接覆蓋。

    3. 複製 `tests/fixtures/test_audio.wav` → `tests/fixtures/v2/sample.wav`（建立 `tests/fixtures/v2/` 目錄）。
  </action>
  <acceptance_criteria>
    - `tests/v2/__init__.py` 檔案存在
    - `tests/v2/test_config.v2.json` 包含 `"database_url": "sqlite:///:memory:"`
    - `tests/v2/test_config.v2.json` 包含 `"enable_webhook": false`
    - `tests/v2/test_config.v2.json` 包含 `"jwt_secret": "test-secret-v2"`
    - `tests/fixtures/v2/sample.wav` 檔案存在且大小 > 0
  </acceptance_criteria>
  <verify>ls -la tests/v2/__init__.py tests/v2/test_config.v2.json tests/fixtures/v2/sample.wav</verify>
  <done>目錄結構與設定檔就緒</done>
</task>

<task type="auto">
  <name>Task 2: 實作 tests/v2/conftest.py — 完整 fixture 集合</name>
  <files>tests/v2/conftest.py</files>
  <read_first>
    - tests/conftest.py（確認 db_engine 使用 StaticPool in-memory，db_session 用 Session(db_engine)）
    - tests/test_external_api_auth.py（學習 client fixture 的 monkeypatch 模式：get_session, JWT_SECRET, OAuth stub）
    - api/auth.py（確認 create_access_token 簽名為 create_access_token(data: dict, expires_delta: timedelta | None = None)）
    - api/routers/tasks.py（確認需 patch 的 import 路徑：api.routers.tasks.get_session, api.routers.tasks.OUTPUT_BASE, api.routers.tasks.log_task_event）
    - api/routers/download.py（確認需 patch：api.routers.download.get_session, api.routers.download.OUTPUT_BASE）
    - api/routers/auth.py（確認 auth router 的 prefix 無 /api/auth — router 自身無 prefix，靠 include_router 設定）
    - pipeline/queue/database.py（確認 get_session 簽名與 reset_engine 存在）
  </read_first>
  <action>
    建立 `tests/v2/conftest.py`，**不重複定義** `db_engine`/`db_session`/`user_fixture`（繼承自 `tests/conftest.py`），只新增以下 fixtures：

    ```python
    """v2 test fixtures — 繼承父層 db_engine/db_session/user_fixture。"""
    import sys
    import types
    from datetime import timedelta

    import pytest
    from fastapi import FastAPI
    from sqlmodel import Session, SQLModel
    from starlette.testclient import TestClient

    from api.auth import create_access_token


    # ── OAuth stub（與 test_external_api_auth.py 相同模式）──
    def _install_oauth_stubs():
        """在 import auth router 前安裝 authlib/google stubs。"""
        class FakeOAuth:
            def __init__(self):
                self.google = types.SimpleNamespace(
                    authorize_access_token=lambda request: {"id_token": "fake"},
                    authorize_redirect=lambda request, callback_url: {"redirect": callback_url},
                )
            def register(self, **kwargs):
                return None

        authlib_module = types.SimpleNamespace(
            integrations=types.SimpleNamespace(
                starlette_client=types.SimpleNamespace(OAuth=FakeOAuth)
            )
        )
        google_auth_module = types.SimpleNamespace(
            transport=types.SimpleNamespace(requests=types.SimpleNamespace(Request=lambda: None))
        )
        google_oauth_module = types.SimpleNamespace(
            id_token=types.SimpleNamespace(verify_oauth2_token=lambda *a, **kw: {})
        )
        sys.modules.setdefault("authlib", authlib_module)
        sys.modules.setdefault("authlib.integrations", authlib_module.integrations)
        sys.modules.setdefault("authlib.integrations.starlette_client", authlib_module.integrations.starlette_client)
        sys.modules.setdefault("google", types.SimpleNamespace(auth=google_auth_module, oauth2=google_oauth_module))
        sys.modules.setdefault("google.auth", google_auth_module)
        sys.modules.setdefault("google.auth.transport", google_auth_module.transport)
        sys.modules.setdefault("google.auth.transport.requests", google_auth_module.transport.requests)
        sys.modules.setdefault("google.oauth2", google_oauth_module)
        sys.modules.setdefault("google.oauth2.id_token", google_oauth_module.id_token)

    _install_oauth_stubs()


    @pytest.fixture
    def client(db_engine, tmp_path, monkeypatch):
        """完整 FastAPI TestClient，包含 auth + tasks + download routers。"""
        # [ISSUE-02 FIX] 確保所有 model 都被 import，
        # 父層 db_engine 只 import Task/StageTask，缺少 TaskEvent/TaskArtifact，
        # 而 tasks router 的 get_task_status 會查詢這兩張表。
        from pipeline.queue.models import Task, StageTask, TaskEvent, TaskArtifact  # noqa: F401
        SQLModel.metadata.create_all(db_engine)

        def _get_session():
            return Session(db_engine)

        # Patch tasks router 依賴
        monkeypatch.setattr("api.routers.tasks.get_session", _get_session)
        monkeypatch.setattr("api.routers.tasks.OUTPUT_BASE", tmp_path)
        monkeypatch.setattr("api.routers.tasks.log_task_event", lambda *a, **kw: None)

        # Patch download router 依賴
        monkeypatch.setattr("api.routers.download.get_session", _get_session)
        monkeypatch.setattr("api.routers.download.OUTPUT_BASE", tmp_path)

        # Patch auth router 依賴
        monkeypatch.setattr("api.routers.auth.get_session", _get_session)

        # Patch pipeline database 依賴
        monkeypatch.setattr("pipeline.queue.database.get_session", _get_session)
        monkeypatch.setattr("pipeline.queue.database.get_engine", lambda: db_engine)

        # Patch JWT secret
        monkeypatch.setenv("JWT_SECRET", "test-secret-v2")
        monkeypatch.setattr("api.auth.JWT_SECRET", "test-secret-v2")

        from api.routers.auth import router as auth_router
        from api.routers.tasks import router as tasks_router
        from api.routers.download import router as download_router

        app = FastAPI()
        app.include_router(auth_router, prefix="/api/auth")
        app.include_router(tasks_router)
        app.include_router(download_router)

        with TestClient(app) as tc:
            yield tc


    @pytest.fixture
    def auth_header(user_fixture):
        """產生 external role 的 Bearer Authorization header。"""
        token = create_access_token(
            {"user_id": str(user_fixture.user_id), "role": "external"}
        )
        return {"Authorization": f"Bearer {token}"}


    @pytest.fixture
    def internal_auth_header(db_session):
        """產生 internal role 的 Bearer Authorization header（不需真實 user）。"""
        token = create_access_token(
            {"user_id": "internal-test-user", "role": "internal"}
        )
        return {"Authorization": f"Bearer {token}"}
    ```

    **關鍵設計決策：**
    - `client` fixture 依賴 `db_engine`（來自父層 `tests/conftest.py`，使用 StaticPool in-memory SQLite）。
    - **[ISSUE-02 FIX]** 在 `SQLModel.metadata.create_all(db_engine)` 前先 import `TaskEvent` 和 `TaskArtifact`，因為父層 `db_engine` 只 import `Task, StageTask`，但 tasks router 的 `get_task_status` 會呼叫 `repo.get_events()` 和 `repo.get_artifacts()`，若表不存在會報錯。
    - `_get_session()` 回傳 `Session(db_engine)` — Session 本身支援 `with` 語法。
    - **不** import `api_server.app`（避免觸發 lifespan 啟動 TaskScheduler）。
    - `auth_header` 依賴 `user_fixture`（來自父層），確保 DB 中有對應的 user 記錄。
    - `internal_auth_header` 不需 DB 中的 user，因為 `verify_token` 只解碼 JWT payload。
    - monkeypatch `api.auth.JWT_SECRET` 是必要的，因為模組載入時 `JWT_SECRET` 已被賦值為 module-level 變數。
  </action>
  <acceptance_criteria>
    - `tests/v2/conftest.py` 包含 `def client(db_engine, tmp_path, monkeypatch):`
    - `tests/v2/conftest.py` 包含 `def auth_header(user_fixture):`
    - `tests/v2/conftest.py` 包含 `def internal_auth_header(db_session):`
    - `tests/v2/conftest.py` 包含 `monkeypatch.setattr("api.routers.tasks.log_task_event", lambda *a, **kw: None)`
    - `tests/v2/conftest.py` 包含 `monkeypatch.setattr("api.routers.tasks.OUTPUT_BASE", tmp_path)`
    - `tests/v2/conftest.py` 包含 `monkeypatch.setattr("api.routers.download.OUTPUT_BASE", tmp_path)`
    - `tests/v2/conftest.py` 包含 `monkeypatch.setattr("api.auth.JWT_SECRET", "test-secret-v2")`
    - `tests/v2/conftest.py` 包含 `from pipeline.queue.models import Task, StageTask, TaskEvent, TaskArtifact`
    - `tests/v2/conftest.py` **不包含** `def db_engine`（繼承父層，不重複定義）
    - `tests/v2/conftest.py` **不包含** `def db_session`（繼承父層，不重複定義）
  </acceptance_criteria>
  <verify>python -c "import ast; ast.parse(open('tests/v2/conftest.py').read()); print('syntax ok')"</verify>
  <done>conftest.py 完成，所有 fixture 已定義</done>
</task>

<task type="auto">
  <name>Task 3: 實作 test_isolation.py — 驗證環境隔離</name>
  <files>tests/v2/test_isolation.py</files>
  <read_first>
    - tests/v2/conftest.py（確認 client, auth_header, internal_auth_header fixture 已就緒）
    - tests/conftest.py（確認 db_engine, db_session, user_fixture fixture 可繼承）
    - pipeline/queue/models.py（確認 Task 與 StageTask model schema）
  </read_first>
  <action>
    建立 `tests/v2/test_isolation.py`，包含以下測試案例：

    ```python
    """驗證 v2 測試環境隔離。"""
    import glob
    import os

    from sqlmodel import select

    from pipeline.queue.models import Task


    def test_db_session_is_in_memory(db_session):
        """驗證 db_session 使用 in-memory SQLite，不產生 .db 檔案。"""
        # 查詢 tasks 表——應該是空的
        tasks = db_session.exec(select(Task)).all()
        assert tasks == []

        # 確認專案根目錄沒有新的 .db 檔案（排除 data/ 下的既有 DB）
        project_root = os.path.dirname(os.path.dirname(os.path.dirname(__file__)))
        new_dbs = glob.glob(os.path.join(project_root, "*.db"))
        assert new_dbs == [], f"Unexpected .db files in project root: {new_dbs}"


    def test_client_fixture_serves_requests(client):
        """驗證 client fixture 能正常處理 HTTP 請求。"""
        # 任意未定義的路由應回 404（不是 500）
        response = client.get("/nonexistent")
        assert response.status_code == 404


    def test_auth_header_is_valid_jwt(client, auth_header):
        """驗證 auth_header 產生的 JWT 能通過 verify_token。"""
        # GET /api/tasks/history 需要有效 token
        response = client.get("/api/tasks/history", headers=auth_header)
        assert response.status_code == 200
        assert isinstance(response.json(), list)


    def test_internal_auth_header_has_internal_role(client, internal_auth_header):
        """驗證 internal_auth_header 的 JWT 包含 role=internal。"""
        from api.auth import verify_token
        token = internal_auth_header["Authorization"].replace("Bearer ", "")
        payload = verify_token(token)
        assert payload["role"] == "internal"


    def test_output_base_is_tmp_path(client, tmp_path):
        """驗證 OUTPUT_BASE 已被 patch 到 tmp_path。

        [ISSUE-08 NOTE] client fixture 內 monkeypatch 設定的 OUTPUT_BASE 指向
        client fixture 自己的 tmp_path 參數。本測試函式的 tmp_path 是另一個獨立的
        暫存目錄（pytest 為每個 fixture/test 提供不同的 tmp_path）。
        因此此處直接檢查 tasks_mod.OUTPUT_BASE 是一個 tmp_path 風格的路徑即可，
        不與本測試的 tmp_path 做 == 比較。
        """
        from api.routers import tasks as tasks_mod
        # client fixture 內已 patch，此處驗證 patch 生效
        # 由於 client 和 test 各自的 tmp_path 可能不同，只驗證型別與非生產路徑
        assert hasattr(tasks_mod.OUTPUT_BASE, "exists")  # 是 Path-like
        assert "output" not in str(tasks_mod.OUTPUT_BASE) or "tmp" in str(tasks_mod.OUTPUT_BASE)
    ```

    **[ISSUE-08 FIX]** `test_output_base_is_tmp_path` 的說明與斷言已更新：
    pytest 的 `tmp_path` 是 per-fixture 獨立的，`client` fixture 拿到的 `tmp_path`
    與 `test_output_base_is_tmp_path` 拿到的 `tmp_path` 是不同的目錄。
    因此不能直接 `assert tasks_mod.OUTPUT_BASE == tmp_path`，
    改為驗證 OUTPUT_BASE 是 Path-like 物件且不是生產路徑。
  </action>
  <acceptance_criteria>
    - `tests/v2/test_isolation.py` 包含 `def test_db_session_is_in_memory(db_session):`
    - `tests/v2/test_isolation.py` 包含 `def test_client_fixture_serves_requests(client):`
    - `tests/v2/test_isolation.py` 包含 `def test_auth_header_is_valid_jwt(client, auth_header):`
    - `tests/v2/test_isolation.py` 包含 `def test_internal_auth_header_has_internal_role(client, internal_auth_header):`
    - `tests/v2/test_isolation.py` 包含 `def test_output_base_is_tmp_path(client, tmp_path):`
    - 執行 `pytest tests/v2/test_isolation.py -v` 全部 5 個測試 PASSED（exit code 0）
  </acceptance_criteria>
  <verify>pytest tests/v2/test_isolation.py -v</verify>
  <done>隔離驗證通過，測試基礎設施就緒</done>
</task>

</tasks>

<verification>
1. `pytest tests/v2/test_isolation.py -v` — 全部 5 個測試 PASSED。
2. `find . -name "*.db" -newer tests/v2/conftest.py` — 不應產生新的 .db 檔案。
3. `grep -c "def db_engine" tests/v2/conftest.py` — 結果為 0（確認不重複定義）。
</verification>

<success_criteria>
- tests/v2/test_isolation.py 所有測試 PASSED。
- conftest.py 提供 client/auth_header/internal_auth_header 三個 fixture。
- 不產生任何 .db 檔案。
</success_criteria>

<output>
After completion, create `.planning/phases/07-test-infrastructure/07-01-SUMMARY.md`
</output>
