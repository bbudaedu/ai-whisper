---
phase: 07-test-infrastructure
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified: [tests/v2/test_auth.py, tests/v2/test_tasks_crud.py, tests/v2/test_download.py]
autonomous: true
requirements: [TEST-01]
must_haves:
  truths:
    - "JWT 簽發與驗證邏輯正確，且過期 Token 會被拒絕"
    - "任務建立後能正確傳回 201 與 UUID，且列表能查詢到該任務"
    - "下載端點能正確產出包含 Word/Excel 的 zip 封裝檔"
  artifacts:
    - path: "tests/v2/test_auth.py"
      provides: "Authentication & Authorization test suite"
    - path: "tests/v2/test_tasks_crud.py"
      provides: "Task management API tests"
    - path: "tests/v2/test_download.py"
      provides: "File format & zip packaging tests"
  key_links:
    - from: "tests/v2/test_auth.py"
      to: "api/auth.py"
      via: "FastAPI OAuth2PasswordRequestForm & jwt.encode/decode"
    - from: "tests/v2/test_download.py"
      to: "api/routers/download.py"
      via: "Verification of zip file structure and filenames"
---

<objective>
實作後端 API 的核心邏輯驗證，確保認證、任務管理與下載功能在 v2.0 中完全穩定。

Purpose: 建立 API 回歸測試基礎，防止後續優化破壞現有合約。
Output: 覆蓋率高的後端 API 整合測試腳本。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@api/auth.py
@api/routers/tasks.py
@api/routers/download.py
@.planning/phases/07-test-infrastructure/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 實作 Auth 與 JWT 驗證測試</name>
  <files>tests/v2/test_auth.py</files>
  <behavior>
    - 正確的認證資訊回傳 200 + access_token。
    - 錯誤的密碼回傳 401 Unauthorized。
    - 無 Token 或 Token 過期呼叫受保護端點回傳 401。
    - 模擬不同的角色（admin/viewer）驗證 RBAC。
  </behavior>
  <action>
    實作 `tests/v2/test_auth.py`。使用 `client` fixture 模擬請求：
    1. 測試 `/api/auth/token` 取得 JWT。
    2. 測試 `/api/auth/me` 取得當前使用者資訊。
    3. 模擬 Token 過期場景。
  </action>
  <verify>pytest tests/v2/test_auth.py -v</verify>
  <done>認證流程驗證通過</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 實作任務 CRUD 與列表測試</name>
  <files>tests/v2/test_tasks_crud.py</files>
  <behavior>
    - 建立任務 (POST /api/tasks) 成功回傳 201 與 task_id。
    - 取得任務列表 (GET /api/tasks) 包含新建立的任務。
    - 取得單一任務詳情 (GET /api/tasks/{id})。
    - 取消任務 (DELETE/PATCH /api/tasks/{id}/cancel) 狀態變更為 cancelled。
  </behavior>
  <action>
    實作 `tests/v2/test_tasks_crud.py`：
    1. 測試各類型的任務建立（音檔上傳 vs YouTube 連結）。
    2. 驗證資料庫欄位 (created_at, status) 的初始值。
    3. 測試非同步狀態輪詢邏輯。
  </action>
  <verify>pytest tests/v2/test_tasks_crud.py -v</verify>
  <done>任務管理 API 邏輯驗證通過</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 實作下載與 Zip 封裝測試</name>
  <files>tests/v2/test_download.py</files>
  <behavior>
    - 下載端點 (GET /api/download/{id}) 回傳 application/zip。
    - Zip 內容包含指定格式的檔案 (Word, Excel, TXT)。
    - 檔案名稱包含 timestamp 或說話者資訊。
    - 遺漏檔案時回傳 404 或提供部分內容。
  </behavior>
  <action>
    實作 `tests/v2/test_download.py`：
    1. 建立一個 `done` 狀態的假任務與對應的 Mock 檔案。
    2. 使用 `zipfile` 模組讀取 Response body 驗證內容。
  </action>
  <verify>pytest tests/v2/test_download.py -v</verify>
  <done>下載邏輯驗證通過</done>
</task>

</tasks>

<verification>
執行 `pytest tests/v2/test_auth.py tests/v2/test_tasks_crud.py tests/v2/test_download.py` 確保全部通過。
</verification>

<success_criteria>
- 所有 API 測試皆能成功執行，無 Flaky 狀況。
- 測試總執行時間 (API 部分) 小於 10 秒。
</success_criteria>

<output>
After completion, create `.planning/phases/07-test-infrastructure/07-02-SUMMARY.md`
</output>
---
