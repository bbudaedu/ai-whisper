---
phase: 07-test-infrastructure
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - tests/v2/test_auth.py
  - tests/v2/test_tasks_crud.py
  - tests/v2/test_download.py
autonomous: true
requirements: [TEST-01]
must_haves:
  truths:
    - "API Key 交換端點 POST /api/auth/token 能正確回傳 access_token 與 refresh_token"
    - "過期 JWT 訪問受保護端點回傳 401 且 detail 包含 Token expired"
    - "external role 的使用者無法查看或取消其他人的任務（404/403）"
    - "internal role 的使用者可以查看所有任務"
    - "YouTube 任務建立回傳 status=pending 與 task_id > 0"
    - "Upload 任務建立會在 OUTPUT_BASE/{task_id}/ 下寫入上傳檔案"
    - "Download 端點對 DONE 任務回傳 application/zip，?format=word 只含 .docx"
    - "Download 端點支援 ?token=xxx query parameter 認證"
  artifacts:
    - path: "tests/v2/test_auth.py"
      provides: "API Key exchange, expired token, RBAC isolation tests"
    - path: "tests/v2/test_tasks_crud.py"
      provides: "Task CRUD: create youtube/upload, get status, cancel, history pagination"
    - path: "tests/v2/test_download.py"
      provides: "Download: zip structure, format alias, token param, RBAC, error codes"
  key_links:
    - from: "tests/v2/test_auth.py"
      to: "api/routers/auth.py"
      via: "POST /api/auth/token with x-api-key header"
    - from: "tests/v2/test_tasks_crud.py"
      to: "api/routers/tasks.py"
      via: "POST /api/tasks/, GET /api/tasks/history, GET /api/tasks/{id}, POST /api/tasks/{id}/cancel"
    - from: "tests/v2/test_download.py"
      to: "api/routers/download.py"
      via: "GET /api/tasks/{id}/download with format filter and zip verification"
---

<objective>
實作後端 API 的核心整合測試：Auth 補缺（API Key、Token 過期、RBAC）、Tasks CRUD（建立/查詢/取消/歷史）、Download（zip 結構/格式篩選/錯誤碼）。

Purpose: 建立 API 回歸測試基礎，覆蓋 TEST-01 的所有子項。
Output: `pytest tests/v2/test_auth.py tests/v2/test_tasks_crud.py tests/v2/test_download.py -v` 全部通過。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/07-test-infrastructure/07-RESEARCH.md（Section 6: Plan 07-02 規劃細節）
@tests/v2/conftest.py
@api/auth.py
@api/routers/auth.py
@api/routers/tasks.py
@api/routers/download.py
@api/models.py
@api/schemas.py
@pipeline/queue/models.py
@pipeline/queue/repository.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 實作 Auth 補缺測試 (tests/v2/test_auth.py)</name>
  <files>tests/v2/test_auth.py</files>
  <read_first>
    - tests/v2/conftest.py（確認 client, auth_header, internal_auth_header fixture 可用）
    - tests/conftest.py（確認 user_fixture 建立 email=test@example.com, role=external, password=StrongPassword123）
    - api/routers/auth.py（確認 /token 端點使用 Depends(api_key_header)，api_key_header name="x-api-key"）
    - api/auth.py（確認 create_access_token 參數格式、verify_token 會 raise HTTPException on expired）
    - api/models.py（確認 ApiKey model: key_hash, user_id, role, is_active）
    - pipeline/queue/repository.py（確認 verify_api_key 方法簽名：接收原始 key，比對 hash）
  </read_first>
  <action>
    建立 `tests/v2/test_auth.py`，包含以下測試案例：

    ```python
    """Auth 補缺測試：API Key 交換、Token 過期、RBAC 隔離。"""
    from datetime import timedelta
    import hashlib

    import pytest
    from sqlmodel import Session

    from api.auth import create_access_token, hash_token
    from api.models import ApiKey


    class TestApiKeyExchange:
        """POST /api/auth/token — x-api-key header 交換 JWT。"""

        def test_valid_api_key_returns_tokens(self, client, db_engine):
            """有效的 API Key 應回傳 access_token 和 refresh_token。"""
            raw_key = "test-api-key-12345"
            with Session(db_engine) as session:
                api_key = ApiKey(
                    key_hash=hash_token(raw_key),
                    user_id="apikey-user-1",
                    role="external",
                    is_active=True,
                )
                session.add(api_key)
                session.commit()

            response = client.post(
                "/api/auth/token",
                headers={"x-api-key": raw_key},
            )
            assert response.status_code == 200
            data = response.json()
            assert "access_token" in data
            assert "refresh_token" in data
            assert data["token_type"] == "bearer"

        def test_invalid_api_key_returns_401(self, client):
            """無效的 API Key 應回傳 401。"""
            response = client.post(
                "/api/auth/token",
                headers={"x-api-key": "nonexistent-key"},
            )
            assert response.status_code == 401

        def test_missing_api_key_returns_401(self, client):
            """缺少 x-api-key header 應回傳 401。"""
            response = client.post("/api/auth/token")
            assert response.status_code == 401


    class TestTokenExpiry:
        """驗證過期 JWT 被拒絕。"""

        def test_expired_token_returns_401(self, client):
            """使用已過期的 JWT 訪問 /api/tasks/history 應回傳 401。"""
            expired_token = create_access_token(
                {"user_id": "test-user", "role": "external"},
                expires_delta=timedelta(seconds=-10),
            )
            response = client.get(
                "/api/tasks/history",
                headers={"Authorization": f"Bearer {expired_token}"},
            )
            assert response.status_code == 401
            assert "expired" in response.json()["detail"].lower()


    class TestRoleBasedAccess:
        """驗證 external/internal 角色隔離。"""

        def test_external_cannot_see_others_task(self, client, auth_header, internal_auth_header, db_engine):
            """external user 查詢他人建立的任務應回 404。"""
            # internal user 建立任務
            response = client.post(
                "/api/tasks/",
                json={
                    "type": "youtube",
                    "payload": {"url": "https://www.youtube.com/watch?v=role_test", "title": "Role Test"},
                },
                headers=internal_auth_header,
            )
            assert response.status_code == 200
            task_id = response.json()["task_id"]

            # external user 嘗試查看（requester 不符）
            response = client.get(f"/api/tasks/{task_id}", headers=auth_header)
            assert response.status_code == 404

        def test_internal_can_see_all_tasks(self, client, auth_header, internal_auth_header):
            """internal user 可以查看所有人的任務。"""
            # external user 建立任務
            response = client.post(
                "/api/tasks/",
                json={
                    "type": "youtube",
                    "payload": {"url": "https://www.youtube.com/watch?v=int_test", "title": "Internal Vis Test"},
                },
                headers=auth_header,
            )
            assert response.status_code == 200
            task_id = response.json()["task_id"]

            # internal user 查看 external 建立的任務
            response = client.get(f"/api/tasks/{task_id}", headers=internal_auth_header)
            assert response.status_code == 200
            assert response.json()["id"] == task_id
    ```

    **重點注意事項：**
    - `hash_token` 使用 `hashlib.sha256`，測試需用同一函式計算 key_hash。
    - `create_access_token` 接受 `expires_delta=timedelta(seconds=-10)` 產生已過期 token。
    - 任務建立端點回傳 200（非 201），因為 `response_model=TaskCreateResponse` 預設 200。
      **[ISSUE-10 NOTE]** RESEARCH.md 表格中提到 201，但實際程式碼回傳 200。以程式碼為準。
  </action>
  <acceptance_criteria>
    - `tests/v2/test_auth.py` 包含 `class TestApiKeyExchange:`
    - `tests/v2/test_auth.py` 包含 `def test_valid_api_key_returns_tokens(`
    - `tests/v2/test_auth.py` 包含 `def test_expired_token_returns_401(`
    - `tests/v2/test_auth.py` 包含 `def test_external_cannot_see_others_task(`
    - `tests/v2/test_auth.py` 包含 `def test_internal_can_see_all_tasks(`
    - `tests/v2/test_auth.py` 包含 `headers={"x-api-key": raw_key}`
    - 執行 `pytest tests/v2/test_auth.py -v` 所有測試 PASSED
  </acceptance_criteria>
  <verify>pytest tests/v2/test_auth.py -v</verify>
  <done>Auth 測試通過</done>
</task>

<task type="auto">
  <name>Task 2: 實作 Tasks CRUD 測試 (tests/v2/test_tasks_crud.py)</name>
  <files>tests/v2/test_tasks_crud.py</files>
  <read_first>
    - tests/v2/conftest.py（確認 client patch 了 log_task_event 為 no-op，OUTPUT_BASE 為 tmp_path）
    - api/routers/tasks.py（確認：POST / 接受 JSON body 含 type/payload/source；GET /history 支援 page/size；POST /{id}/cancel 用 repo.cancel_task）
    - api/schemas.py（確認 TaskCreateResponse 欄位：task_id int, status str, created_at datetime）
    - pipeline/queue/models.py（確認 TaskStatus.PENDING = "pending", TaskStatus.CANCELED = "canceled"）
    - pipeline/queue/repository.py（確認 cancel_task：unauthorized → {"status": "error", "reason": "unauthorized"}，tasks.py 會轉為 HTTP 403）
  </read_first>
  <action>
    建立 `tests/v2/test_tasks_crud.py`，包含以下測試案例：

    ```python
    """Tasks CRUD API 測試。"""
    import io
    from pathlib import Path

    import pytest


    class TestCreateTask:
        """POST /api/tasks/ — 建立任務。"""

        def test_create_youtube_task(self, client, auth_header):
            """JSON body type=youtube 應回傳 task_id 與 status=pending。"""
            response = client.post(
                "/api/tasks/",
                json={
                    "type": "youtube",
                    "payload": {
                        "url": "https://www.youtube.com/watch?v=yt_crud_test",
                        "title": "CRUD 測試影片",
                    },
                },
                headers=auth_header,
            )
            assert response.status_code == 200
            data = response.json()
            assert data["task_id"] > 0
            assert data["status"] == "pending"
            assert "created_at" in data

        def test_create_upload_task(self, client, auth_header):
            """multipart upload type=upload 應建立任務並寫入檔案。

            [ISSUE-NEW-01 FIX] 不使用測試函式的 tmp_path 參數，改從已 patch 的
            api.routers.tasks.OUTPUT_BASE 取得正確路徑（與 ISSUE-B done_task_with_files 修法一致）。
            """
            import api.routers.tasks as tasks_mod

            wav_content = b"RIFF" + b"\x00" * 100  # 假的 WAV header
            response = client.post(
                "/api/tasks/",
                data={"type": "upload", "payload": '{"title": "Upload Test"}'},
                files={"file": ("test.wav", io.BytesIO(wav_content), "audio/wav")},
                headers=auth_header,
            )
            assert response.status_code == 200
            data = response.json()
            task_id = data["task_id"]
            assert task_id > 0

            # 從已 patch 的 OUTPUT_BASE 取得正確路徑
            upload_dir = tasks_mod.OUTPUT_BASE / str(task_id)
            assert upload_dir.exists()
            uploaded_files = list(upload_dir.iterdir())
            assert len(uploaded_files) == 1
            assert uploaded_files[0].name == "test.wav"

        def test_create_task_missing_url_returns_400(self, client, auth_header):
            """YouTube 任務缺少 url 應回 400。"""
            response = client.post(
                "/api/tasks/",
                json={"type": "youtube", "payload": {"title": "No URL"}},
                headers=auth_header,
            )
            assert response.status_code == 400

        def test_create_task_invalid_type_returns_400(self, client, auth_header):
            """無效的 task type 應回 400。"""
            response = client.post(
                "/api/tasks/",
                json={"type": "invalid", "payload": {}},
                headers=auth_header,
            )
            assert response.status_code == 400

        def test_create_task_without_auth_returns_401(self, client):
            """無 token 建立任務應回 401。"""
            response = client.post(
                "/api/tasks/",
                json={"type": "youtube", "payload": {"url": "https://youtu.be/x"}},
            )
            assert response.status_code in (401, 403)


    class TestGetTask:
        """GET /api/tasks/{id} 與 GET /api/tasks/history。"""

        def test_get_own_task(self, client, auth_header):
            """external user 可以查看自己建立的任務。"""
            create_resp = client.post(
                "/api/tasks/",
                json={
                    "type": "youtube",
                    "payload": {"url": "https://youtu.be/own_task", "title": "Own Task"},
                },
                headers=auth_header,
            )
            task_id = create_resp.json()["task_id"]

            response = client.get(f"/api/tasks/{task_id}", headers=auth_header)
            assert response.status_code == 200
            data = response.json()
            assert data["id"] == task_id
            assert data["title"] == "Own Task"
            assert data["status"] == "pending"

        def test_get_nonexistent_task_returns_404(self, client, auth_header):
            """查詢不存在的 task_id 應回 404。"""
            response = client.get("/api/tasks/99999", headers=auth_header)
            assert response.status_code == 404

        def test_task_history_returns_list(self, client, auth_header):
            """GET /api/tasks/history 應回傳列表。"""
            # 先建立兩個任務
            for i in range(2):
                client.post(
                    "/api/tasks/",
                    json={
                        "type": "youtube",
                        "payload": {"url": f"https://youtu.be/hist_{i}", "title": f"History {i}"},
                    },
                    headers=auth_header,
                )

            response = client.get("/api/tasks/history", headers=auth_header)
            assert response.status_code == 200
            data = response.json()
            assert isinstance(data, list)
            assert len(data) >= 2

        def test_task_history_pagination(self, client, auth_header):
            """GET /api/tasks/history?page=1&size=1 應只回傳 1 筆。"""
            for i in range(3):
                client.post(
                    "/api/tasks/",
                    json={
                        "type": "youtube",
                        "payload": {"url": f"https://youtu.be/page_{i}", "title": f"Page {i}"},
                    },
                    headers=auth_header,
                )

            response = client.get("/api/tasks/history?page=1&size=1", headers=auth_header)
            assert response.status_code == 200
            data = response.json()
            assert len(data) == 1


    class TestCancelTask:
        """POST /api/tasks/{id}/cancel。"""

        def test_cancel_own_task(self, client, auth_header):
            """取消自己的任務應成功。"""
            create_resp = client.post(
                "/api/tasks/",
                json={
                    "type": "youtube",
                    "payload": {"url": "https://youtu.be/cancel_me", "title": "Cancel Me"},
                },
                headers=auth_header,
            )
            task_id = create_resp.json()["task_id"]

            response = client.post(f"/api/tasks/{task_id}/cancel", headers=auth_header)
            assert response.status_code == 200
            data = response.json()
            assert data["status"] in ("canceled", "cancelled")

        def test_cancel_others_task_returns_403(self, client, auth_header, internal_auth_header):
            """external user 取消其他人的任務應回 403。

            [ISSUE-07 VERIFIED] 確認行為：
            - repository.cancel_task() 回傳 {"status": "error", "reason": "unauthorized"}
            - tasks.py cancel_task endpoint 檢測到此結果後 raise HTTPException(403)
            - 因此 HTTP response 確實是 403，非 200+error dict。
            """
            # internal user 建立任務
            create_resp = client.post(
                "/api/tasks/",
                json={
                    "type": "youtube",
                    "payload": {"url": "https://youtu.be/not_mine", "title": "Not Mine"},
                },
                headers=internal_auth_header,
            )
            task_id = create_resp.json()["task_id"]

            # external user 嘗試取消
            response = client.post(f"/api/tasks/{task_id}/cancel", headers=auth_header)
            assert response.status_code == 403
    ```

    **重點注意事項：**
    - `POST /api/tasks/` 回傳 status code 200（非 201），因為 FastAPI router 的 `response_model` 預設 200。
    - `log_task_event` 已被 conftest.py patch 為 no-op。
    - `OUTPUT_BASE` 已被 patch 為 `tmp_path`，upload 測試可驗證檔案寫入。
    - **[ISSUE-07 VERIFIED]** `cancel_task` 的 HTTP response：`repository.cancel_task()` 回傳 dict，但 `tasks.py` 的 endpoint 會檢查 `result.get("status") == "error" and result.get("reason") == "unauthorized"` 並 raise `HTTPException(status_code=403)`。因此外部看到的是 HTTP 403，不是 200+error dict。斷言 `response.status_code == 403` 正確。
  </action>
  <acceptance_criteria>
    - `tests/v2/test_tasks_crud.py` 包含 `class TestCreateTask:`
    - `tests/v2/test_tasks_crud.py` 包含 `def test_create_youtube_task(`
    - `tests/v2/test_tasks_crud.py` 包含 `def test_create_upload_task(`
    - `tests/v2/test_tasks_crud.py` 包含 `def test_get_own_task(`
    - `tests/v2/test_tasks_crud.py` 包含 `def test_task_history_pagination(`
    - `tests/v2/test_tasks_crud.py` 包含 `def test_cancel_own_task(`
    - `tests/v2/test_tasks_crud.py` 包含 `def test_cancel_others_task_returns_403(`
    - 執行 `pytest tests/v2/test_tasks_crud.py -v` 所有測試 PASSED
  </acceptance_criteria>
  <verify>pytest tests/v2/test_tasks_crud.py -v</verify>
  <done>Tasks CRUD 測試通過</done>
</task>

<task type="auto">
  <name>Task 3: 實作 Download 測試 (tests/v2/test_download.py)</name>
  <files>tests/v2/test_download.py</files>
  <read_first>
    - tests/v2/conftest.py（確認 client fixture 已 patch OUTPUT_BASE 為 tmp_path；確認 download router 已 include）
    - api/routers/download.py（確認：GET /{task_id}/download 端點；format_map 含 word→.docx, excel→.xlsx；RBAC 檢查順序：先查 task → RBAC（role != "internal" and task.requester != requester → 403）→ status check（status != DONE → 400）→ 找檔案；ALLOWED_SUFFIXES = {".srt", ".txt", ".xlsx", ".docx", ".vtt", ".json", ".tsv"}；支援 ?token=xxx query param）
    - pipeline/queue/models.py（確認 Task model 的 video_id, status, requester 欄位）
    - api/routers/download.py（確認搜尋策略：先 OUTPUT_BASE/{video_id}/，再 OUTPUT_BASE/{task_id}/）
  </read_first>
  <action>
    建立 `tests/v2/test_download.py`，包含以下測試案例：

    ```python
    """Download 端點測試：zip 結構、格式篩選、認證與 RBAC。"""
    import io
    import zipfile

    import pytest
    from sqlmodel import Session

    from api.auth import create_access_token
    from pipeline.queue.models import Task, TaskStatus, TaskSource


    @pytest.fixture
    def done_task_with_files(client, db_engine):
        """建立 DONE 狀態的任務與對應的輸出檔案。

        [ISSUE-12 VERIFIED] ALLOWED_SUFFIXES = {".srt", ".txt", ".xlsx", ".docx", ".vtt", ".json", ".tsv"}
        .srt 確實在 ALLOWED_SUFFIXES 內，因此 fixture 中建立的 4 個檔案（.docx, .xlsx, .txt, .srt）
        全部都會被收集。zip 斷言數量 == 4 是正確的。

        [ISSUE-B FIX] 依賴 client fixture 而非獨立的 tmp_path。
        Pytest 為每個 fixture 提供獨立的 tmp_path，若 done_task_with_files 使用自己的 tmp_path，
        會與 client fixture patch 給 download router 的 OUTPUT_BASE 不同目錄，導致 download
        端點找不到檔案而 404。改為從已 patch 的 api.routers.download.OUTPUT_BASE 取得正確路徑。
        """
        import api.routers.download as download_mod
        output_base = download_mod.OUTPUT_BASE  # 已被 client fixture patch 為其 tmp_path

        with Session(db_engine) as session:
            task = Task(
                title="Download Test",
                video_id="dl_test_vid",
                status=TaskStatus.DONE,
                requester="test-requester-dl",
                source=TaskSource.EXTERNAL,
            )
            session.add(task)
            session.commit()
            session.refresh(task)

            # 建立輸出檔案（使用 task.id 路徑，因為 download 會搜尋 OUTPUT_BASE/{task_id}/）
            output_dir = output_base / str(task.id)
            output_dir.mkdir(parents=True, exist_ok=True)
            (output_dir / "result.docx").write_bytes(b"fake word content")
            (output_dir / "result.xlsx").write_bytes(b"fake excel content")
            (output_dir / "result.txt").write_text("fake text content")
            (output_dir / "result.srt").write_text("1\n00:00:00,000 --> 00:00:01,000\ntest\n")

            return task


    @pytest.fixture
    def dl_auth_header():
        """產生 requester=test-requester-dl 的 auth header。"""
        token = create_access_token(
            {"user_id": "test-requester-dl", "role": "external"}
        )
        return {"Authorization": f"Bearer {token}"}


    class TestDownloadFormats:
        """GET /api/tasks/{id}/download — 格式篩選。"""

        def test_download_all_formats(self, client, done_task_with_files, dl_auth_header):
            """無 format 參數，zip 應包含所有 ALLOWED_SUFFIXES 的檔案。"""
            task = done_task_with_files
            response = client.get(f"/api/tasks/{task.id}/download", headers=dl_auth_header)
            assert response.status_code == 200
            assert response.headers["content-type"] == "application/zip"

            zf = zipfile.ZipFile(io.BytesIO(response.content))
            names = zf.namelist()
            assert len(names) == 4  # docx, xlsx, txt, srt — 全部在 ALLOWED_SUFFIXES 中
            assert any(n.endswith(".docx") for n in names)
            assert any(n.endswith(".xlsx") for n in names)
            assert any(n.endswith(".txt") for n in names)
            assert any(n.endswith(".srt") for n in names)

        def test_download_word_alias(self, client, done_task_with_files, dl_auth_header):
            """?format=word 應只回傳 .docx 檔案。"""
            task = done_task_with_files
            response = client.get(
                f"/api/tasks/{task.id}/download?format=word",
                headers=dl_auth_header,
            )
            assert response.status_code == 200
            zf = zipfile.ZipFile(io.BytesIO(response.content))
            names = zf.namelist()
            assert len(names) == 1
            assert names[0].endswith(".docx")

        def test_download_excel_alias(self, client, done_task_with_files, dl_auth_header):
            """?format=excel 應只回傳 .xlsx 檔案。"""
            task = done_task_with_files
            response = client.get(
                f"/api/tasks/{task.id}/download?format=excel",
                headers=dl_auth_header,
            )
            assert response.status_code == 200
            zf = zipfile.ZipFile(io.BytesIO(response.content))
            names = zf.namelist()
            assert len(names) == 1
            assert names[0].endswith(".xlsx")

        def test_download_txt_format(self, client, done_task_with_files, dl_auth_header):
            """?format=txt 應只回傳 .txt 檔案。"""
            task = done_task_with_files
            response = client.get(
                f"/api/tasks/{task.id}/download?format=txt",
                headers=dl_auth_header,
            )
            assert response.status_code == 200
            zf = zipfile.ZipFile(io.BytesIO(response.content))
            names = zf.namelist()
            assert len(names) == 1
            assert names[0].endswith(".txt")


    class TestDownloadAuth:
        """Download 端點認證機制。"""

        def test_download_with_token_query_param(self, client, done_task_with_files):
            """?token=xxx query param 應等同 Bearer header。"""
            task = done_task_with_files
            token = create_access_token(
                {"user_id": "test-requester-dl", "role": "external"}
            )
            response = client.get(f"/api/tasks/{task.id}/download?token={token}")
            assert response.status_code == 200
            assert response.headers["content-type"] == "application/zip"

        def test_download_without_auth_returns_401(self, client, done_task_with_files):
            """無 token 下載應回 401。"""
            task = done_task_with_files
            response = client.get(f"/api/tasks/{task.id}/download")
            assert response.status_code == 401


    class TestDownloadRBAC:
        """Download 端點角色隔離。"""

        def test_download_forbidden_for_other_external_user(self, client, done_task_with_files, auth_header):
            """external user 下載別人的任務應回 403。"""
            task = done_task_with_files
            # auth_header 的 user_id 與 task.requester="test-requester-dl" 不同
            response = client.get(f"/api/tasks/{task.id}/download", headers=auth_header)
            assert response.status_code == 403

        def test_download_allowed_for_internal(self, client, done_task_with_files, internal_auth_header):
            """internal user 可以下載任何人的任務。"""
            task = done_task_with_files
            response = client.get(f"/api/tasks/{task.id}/download", headers=internal_auth_header)
            assert response.status_code == 200


    class TestDownloadErrors:
        """Download 端點錯誤情境。"""

        def test_download_pending_task_returns_400(self, client, db_engine, internal_auth_header):
            """status=PENDING 的任務下載應回 400。

            [ISSUE-04 FIX] download.py 的檢查順序為：
            1. 查 task（不存在 → 404）
            2. RBAC（role != "internal" and requester 不符 → 403）
            3. status check（status != DONE → 400）

            因此若要測 status check（400），必須先通過 RBAC。
            使用 internal_auth_header 可直接繞過 RBAC，確保測到 status check 邏輯。
            不再使用 auth_header + _extract_user_id，避免 RBAC 先回 403。
            """
            with Session(db_engine) as session:
                task = Task(
                    title="Pending Task",
                    video_id="pending_vid",
                    status=TaskStatus.PENDING,
                    requester="some-other-user",
                    source=TaskSource.EXTERNAL,
                )
                session.add(task)
                session.commit()
                session.refresh(task)
                task_id = task.id

            response = client.get(f"/api/tasks/{task_id}/download", headers=internal_auth_header)
            assert response.status_code == 400
            assert "not completed" in response.json()["detail"].lower()

        def test_download_done_but_no_files_returns_404(self, client, db_engine, tmp_path):
            """DONE 任務但無輸出檔案應回 404。"""
            with Session(db_engine) as session:
                task = Task(
                    title="Empty Done",
                    video_id="empty_done_vid",
                    status=TaskStatus.DONE,
                    requester="empty-user",
                    source=TaskSource.EXTERNAL,
                )
                session.add(task)
                session.commit()
                session.refresh(task)
                task_id = task.id

            token = create_access_token({"user_id": "empty-user", "role": "external"})
            response = client.get(
                f"/api/tasks/{task_id}/download",
                headers={"Authorization": f"Bearer {token}"},
            )
            assert response.status_code == 404


    class TestZipStructure:
        """驗證 zip 封裝結構。"""

        def test_zip_arcname_is_relative(self, client, done_task_with_files, dl_auth_header):
            """zip 內的 arcname 應為相對路徑（不含絕對路徑前綴）。"""
            task = done_task_with_files
            response = client.get(f"/api/tasks/{task.id}/download", headers=dl_auth_header)
            zf = zipfile.ZipFile(io.BytesIO(response.content))
            for name in zf.namelist():
                assert not name.startswith("/"), f"arcname should be relative: {name}"
                assert ".." not in name, f"arcname should not contain ..: {name}"
    ```

    **重點注意事項：**
    - **[ISSUE-B FIX]** `done_task_with_files` fixture 依賴 `client` fixture（非獨立 `tmp_path`），從已 patch 的 `api.routers.download.OUTPUT_BASE` 取得正確的 tmp_path。這確保 fixture 建立的檔案與 download router 搜尋的路徑一致，避免 Pytest 為不同 fixture 產生不同 `tmp_path` 導致 404。
    - Download router 搜尋策略：先找 `OUTPUT_BASE/{video_id}/`，再找 `OUTPUT_BASE/{task_id}/`。fixture 使用 `task.id` 路徑。
    - `dl_auth_header` 的 user_id 必須與 `done_task_with_files` 的 `task.requester` 一致（"test-requester-dl"）。
    - `auth_header`（來自 conftest）的 user_id 是 `user_fixture.user_id`（UUID），與 "test-requester-dl" 不同，用於測試 RBAC。
    - **[ISSUE-04 FIX]** `test_download_pending_task_returns_400` 改用 `internal_auth_header` 避開 RBAC（download.py 先檢查 RBAC 再檢查 status），確保直接測到 status check 回 400。
    - **[ISSUE-12 VERIFIED]** `.srt` 確實在 `ALLOWED_SUFFIXES` 中，fixture 的 4 個檔案全部會被收集，斷言 `len(names) == 4` 正確。
  </action>
  <acceptance_criteria>
    - `tests/v2/test_download.py` 包含 `class TestDownloadFormats:`
    - `tests/v2/test_download.py` 包含 `def test_download_all_formats(`
    - `tests/v2/test_download.py` 包含 `def test_download_word_alias(`
    - `tests/v2/test_download.py` 包含 `def test_download_with_token_query_param(`
    - `tests/v2/test_download.py` 包含 `def test_download_forbidden_for_other_external_user(`
    - `tests/v2/test_download.py` 包含 `def test_download_pending_task_returns_400(`
    - `tests/v2/test_download.py` 包含 `def test_zip_arcname_is_relative(`
    - `tests/v2/test_download.py` 包含 `zipfile.ZipFile(io.BytesIO(response.content))`
    - `test_download_pending_task_returns_400` 使用 `internal_auth_header`（非 auth_header）
    - `done_task_with_files` fixture 依賴 `client`（非獨立 `tmp_path`），從 `download_mod.OUTPUT_BASE` 取路徑
    - 執行 `pytest tests/v2/test_download.py -v` 所有測試 PASSED
  </acceptance_criteria>
  <verify>pytest tests/v2/test_download.py -v</verify>
  <done>Download 測試通過</done>
</task>

</tasks>

<verification>
1. `pytest tests/v2/test_auth.py tests/v2/test_tasks_crud.py tests/v2/test_download.py -v` — 全部 PASSED。
2. 確認 `pytest tests/v2/test_auth.py --co -q | wc -l` >= 6（至少 6 個 auth 測試）。
3. 確認 `pytest tests/v2/test_tasks_crud.py --co -q | wc -l` >= 9（至少 9 個 CRUD 測試）。
4. 確認 `pytest tests/v2/test_download.py --co -q | wc -l` >= 9（至少 9 個 download 測試）。
5. 執行時間應 < 10 秒。
</verification>

<success_criteria>
- 所有 API 測試 PASSED，無 flaky。
- Auth 覆蓋 API Key 交換、Token 過期、RBAC 隔離。
- Tasks CRUD 覆蓋建立（youtube/upload）、查詢、取消、歷史分頁。
- Download 覆蓋格式篩選、zip 結構、token query param、RBAC、錯誤碼。
</success_criteria>

<output>
After completion, create `.planning/phases/07-test-infrastructure/07-02-SUMMARY.md`
</output>
