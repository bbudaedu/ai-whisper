---
phase: 07-test-infrastructure
plan: 03
type: execute
wave: 3
depends_on: [07-02]
files_modified: [tests/v2/test_pipeline.py, tests/v2/test_gpu_concurrency.py, tests/v2/test_smoke_e2e.py]
autonomous: true
requirements: [TEST-02]
must_haves:
  truths:
    - "Pipeline 節點 (Whisper/Proofreading) 錯誤時會觸發 retry 或正確記錄失敗狀態"
    - "多任務提交時 GPU Lock 能有效序列化任務執行順序"
    - "5 秒音檔 Smoke Test 能在 15 秒內完成端對端流程 (Mock GPU)"
  artifacts:
    - path: "tests/v2/test_pipeline.py"
      provides: "Pipeline state machine & retry logic tests"
    - path: "tests/v2/test_gpu_concurrency.py"
      provides: "Concurrency & GPU locking tests"
  key_links:
    - from: "tests/v2/test_gpu_concurrency.py"
      to: "pipeline/queue/gpu_lock.py"
      via: "File locking & semaphore verification"
    - from: "tests/v2/test_pipeline.py"
      to: "pipeline/auto_youtube_whisper.py"
      via: "Verification of task state progression (pending -> running -> done)"
---

<objective>
驗證語音處理管線 (Pipeline) 的狀態轉換、異常處理與 GPU 並發控制邏輯。

Purpose: 確保 Pipeline 在高負載或異常輸入下仍能維持系統穩定且資料一致。
Output: 覆蓋 Pipeline 核心演算法與排程邏輯的測試案例。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@pipeline/queue/models.py
@pipeline/queue/gpu_lock.py
@pipeline/auto_youtube_whisper.py
@.planning/phases/07-test-infrastructure/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 實作 Pipeline 狀態機與 Retry 測試</name>
  <files>tests/v2/test_pipeline.py</files>
  <behavior>
    - 任務進入 Pipeline 時狀態變更為 running。
    - 若節點拋出特定異常 (Transient error)，應自動重新執行 (如有 retry 機制)。
    - 若節點致命失敗，狀態變更為 failed 並記錄正確的 error_log。
    - 任務完成後，資料庫中的 processed_path 應被正確更新。
  </behavior>
  <action>
    實作 `tests/v2/test_pipeline.py`：
    1. 使用 Mock 攔截 `auto_youtube_whisper.run_whisper`。
    2. 模擬成功、重試與失敗三種情境。
    3. 驗證資料庫中的任務狀態轉換。
  </action>
  <verify>pytest tests/v2/test_pipeline.py -v</verify>
  <done>Pipeline 狀態機邏輯驗證通過</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 實作 GPU 並發鎖定 (GPU Lock) 測試</name>
  <files>tests/v2/test_gpu_concurrency.py</files>
  <behavior>
    - 當一個進程持有 GPU Lock 時，其他請求鎖定的進程應阻塞。
    - 進程崩潰或超時後，GPU Lock 應能自動釋放 (或透過清理機制釋放)。
    - 支援多 GPU 分配時，應能根據 index 正確分流。
  </behavior>
  <action>
    實作 `tests/v2/test_gpu_concurrency.py`：
    1. 使用 `threading` 或 `multiprocessing` 模擬多個進程爭搶 `gpu_lock.py` 中的資源。
    2. 檢查 lock file 是否存在。
    3. 驗證序列化執行（執行時間應等於單次執行時間 * 任務數）。
  </action>
  <verify>pytest tests/v2/test_gpu_concurrency.py -v</verify>
  <done>GPU 並發控制邏輯驗證通過</done>
</task>

<task type="auto">
  <name>Task 3: 實作端對端 Smoke Test (Mock GPU 模式)</name>
  <files>tests/v2/test_smoke_e2e.py</files>
  <action>
    實作一個輕量級的 E2E 測試：
    1. 使用 `TestClient` 提交任務 (sample.wav)。
    2. 手動觸發 Pipeline (Mock GPU 模式，不執行真實 Whisper)。
    3. 等待任務狀態轉變為 `done`。
    4. 驗證最終輸出路徑是否有產出檔案。
  </action>
  <verify>pytest tests/v2/test_smoke_e2e.py -v</verify>
  <done>系統核心路徑 (API -> Pipeline -> Data) 連通性驗證通過</done>
</task>

</tasks>

<verification>
執行 `pytest tests/v2` 確保所有 07-03 的測試案例皆能通過，且無懸掛 (hanging) 狀況。
</verification>

<success_criteria>
- 所有 Pipeline 測試案例通過。
- 測試總執行時間 (全套) 小於 30 秒。
</success_criteria>

<output>
After completion, create `.planning/phases/07-test-infrastructure/07-03-SUMMARY.md`
</output>
---
