# Continue Here: Phase 06 - 里程碑缺口修復

**Date:** 2026-03-24
**Phase:** 06-milestone-gap-closure
**Status:** In Progress (Wave 2 Completed, Wave 3 Pending)

## Current Context
正在執行 Phase 06 里程碑缺口修復，旨在解決 v1.0 稽核中發現的整合問題。已完成前端路徑修正與後端格式相容性增強。

## Completed Work
- [x] **Wave 1: API 路徑與格式名稱修復**
    - `TaskHistory.tsx` & `TaskTracker.tsx`: 修正 `downloadUrl` 邏輯確保包含 `/api`，對齊 `usePolling` endpoint。
    - `SubmitTask.tsx`: 將格式選擇由 `docx`/`xlsx` 改為 `word`/`excel` 以對齊用戶語意。
- [x] **Wave 2: 狀態枚舉與下載邏輯增強**
    - `TaskHistory.tsx` & `TaskTracker.tsx`: 更新 `TaskRecord` 狀態型別與 `getStatusBadge` 邏輯，對齊後端 `TaskStatus` (包含 `queued`, `running`, `canceled`)。
    - `api/routers/download.py`: 增加 `format_map` 支援 `word` -> `.docx` 與 `excel` -> `.xlsx` 的別名映射。

## Remaining Work
- [ ] **Wave 3: 文件與進度同步**
    - 更新 `REQUIREMENTS.md`: 將所有 v1 需求標記為完成並更新 Traceability。
    - 更新 `PROJECT.md`: 將 `Active` 區塊項目移至 `Validated`。
    - 更新 `.planning/STATE.md`: 將 Phase 指向 06 並標記為完成。
- [ ] **Final Verification**
    - 執行 `npm run build` 確保前端編譯無誤。
    - 最終里程碑確認。

## Decisions Made
- 後端 `download.py` 採映射表方式處理格式別名，既維持了檔案系統的 `.docx` 副檔名，又解決了前端 UI `word` 標籤的對接問題。
- 前端 `TaskStatus` 型別擴充為聯集型別，並使用 `as any` 處理部分轉型，確保在不改動大量基礎型別的前提下對齊後端枚舉。

## How to Resume
1. 讀取 `.planning/phases/06-milestone-gap-closure/06-01-PLAN.md`。
2. 繼續執行 Wave 3：同步專案文件（REQUIREMENTS.md, PROJECT.md）。
3. 完成 Phase 06 驗證後結束里程碑。
